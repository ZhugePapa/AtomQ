# 数据综合 (或数据抽象)

## 原文要点

- ==数据综合 (或数据抽象) 指由高精度数据经过抽象形成精度较低但是粒度较大的数据。==
- ==其作用过程为从多个较高精度的局部数据中, 获得较低精度的全局数据。在这个过程中，==
- 多粒度数据| 要对各局域中的数据进行综合，提取其主要特征。
- 集成      2. 数据综合集成的过程是特征提取和归并的过程。 数据细化指通过由一定精度的数据获
- 取精度较高的数据，实现该过程的主要途径有: 时空转换，相关分析或者由综合中数据
- 变动的记录进行恢复。数据集成是最终实现数据共享和辅助决策的基础。