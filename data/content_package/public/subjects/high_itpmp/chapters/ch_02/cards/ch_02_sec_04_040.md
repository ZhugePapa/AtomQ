### 概述

分布式账本的核心思想是：交易记账由分布在不同地方的多个节点共同完成，每个节点保存账本副本，并共同监督交易合法性。

### 不可篡改机制

区块链用带时间戳的链式结构存储数据。后序区块会验证前序区块，如果篡改某一区块，就需要递归修改该区块及所有后续区块，并重新计算哈希，代价极高。

### 访问与更新

分布式账本一方面通过公私钥和签名控制账本访问权；另一方面根据共识规则，由一个、一部分或所有参与者共同完成信息更新。

### 易混点

不可篡改不是“物理上绝对不能修改”，而是修改成本和共识约束使篡改在实际系统中不可行。
