#### 概述

==典型代表特征==。

#### 核心内容

- 典型代表特征
- 对称密钥体制数据加密标准 ==DES==算法加密密钥和解密密钥相同非对称密钥体制
- ==RSA== 算法加密密钥和解密密钥不同，加密密钥可以公开而解密密钥需
- 要保密用户和实体行为分析(==UBBA==) 提供了用户画像及基于各种分析方法的异常检测，结合基本分析方法(利用签名的规则、模式匹配、简单统计、阈值等)和高级分析方法(监督和无监督的机器学习等)，用打包分析来评估用户和其他实体(主机、应用程序、网络、数据库等)，发现与用户
