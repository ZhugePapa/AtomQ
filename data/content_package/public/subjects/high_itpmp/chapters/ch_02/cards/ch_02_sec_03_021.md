### 概述

加密技术包括两个元素：==算法==和==密钥==。发送方将明文加密成密文，接收方使用解密密钥恢复明文。

### 密钥体制对照

| 类型 | 特征 | 典型代表 |
|:---|:---|:---|
| 对称密钥体制 | 加密密钥和解密密钥相同 | DES |
| 非对称密钥体制 | 加密密钥和解密密钥不同，加密密钥可公开，解密密钥需保密 | RSA |

### 易混点

“密钥是否相同”是判断对称/非对称的关键；不要只看算法名字。
