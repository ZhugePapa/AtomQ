#### 概述

==算法意==：故列算法也叫数据搞要或者哈希算法也被理解為该数据的"指纹信典型的散列算法有 ==MD==5、==SHA==-1/==SHA==-2 和 ==SM==3，目前区块链主要使用==SHA==-2 中的 ==SHA256 算法==。

#### 核心内容

- 常用的非对称加密算法包括RSA==、==EIgamakD-H==、==ECC(椭圆曲线加密算==- 法)等==。
- 共识算法能保证分布式的计算机或软件程序协作一致，对系统的==输入==输出做出正确的响应。
- 区块链的共识机制的思想是：在没有中心点总体协调的情况下，当某个记账节点提议区块数据增加或减少，并把该提议广播给所有的参与节点，所有节点要根据一定的规则和机制，对这一提议是否能够达成一致进行计算和处理。
==- 目前==，常用的共识机制主要有PoW==、==PoS==、==D==PoS==、==Paxos==、==PBFT 等==。
- 根据区块链不同应用场景各种共识机制的特性，共识机制分析可基
- 丁：
- 合规监管：是否支持超级权限节点对全网节点、数据进行监
- 管。
- 性能效率：交易达成共识被确认的效率。
- ==资源消耗==：共识过程中耗费的==CPU==网络输入输出、存储等资源。
- ==容错性==：防攻击、防欺诈的能力虚抄实

#### 考试抓手

- ==资源消耗==、==CPU==
- ==ECC(椭圆曲线加密算==、==EIgamakD-H==、==RSA==
- ==PBFT 等==、==Paxos==、==DPoS==、==PoW==、==PoS==
- ==容错性==
