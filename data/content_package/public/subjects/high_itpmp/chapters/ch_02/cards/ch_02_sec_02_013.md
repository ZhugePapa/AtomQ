### 概述

数据库根据存储方式可分为关系型数据库（SQL）和非关系型数据库（NoSQL）。

### 对照

| 类型 | 特征 |
|:---|:---|
| 关系型数据库 | 支持事务 ACID 原则，用二维表组织数据 |
| NoSQL | 分布式、非关系型，不保证遵循 ACID 原则 |

### NoSQL 的常见特点

NoSQL 数据存储不需要固定表结构，通常也不存在连接操作，适合大规模、高并发、结构灵活的数据场景。

### 易混点

NoSQL 不是“不用数据库”，而是不以传统关系表作为主要数据模型。
