### 概述

==使用SA方法进行需求分析== 是本章需要掌握的考点之一。4、使用SA方法进行需求分析，其建立的模型的核心是数据字典，围绕这个核心，有三个层次

### 结构化要点

- 的模型，分别是数据模型、功能模型和行模型(也称状态模型)。(22上9)
- 模型
- 数据模型
- 功能模型
- 表示图形
- 表示图形的作用
- 实体联系图(E-R图)

:::key
1. 4、使用SA方法进行需求分析，其建立的模型的核心是数据字典，围绕这个核心，有三个层次
2. 的模型，分别是数据模型、功能模型和行模型(也称状态模型)。(22上9)
3. 表示图形的作用
:::

:::warning
本卡片由 OCR 文本自动整理，`review_status` 为 `ai_draft`，上线前需要人工核对原文、表格和红色重点。
:::
