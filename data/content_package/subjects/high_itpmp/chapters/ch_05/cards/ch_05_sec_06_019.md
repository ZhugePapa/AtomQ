### 概述

==根据模型应用目的不同== 是本章需要掌握的考点之一。1、根据模型应用目的不同，可以将数据模型划分三类：概念模型、逻辑模型和物理模型。

### 结构化要点

- 模型
- 概念
- 模型
- 逻辑
- 解释说明
- 信息模型，它是按用户的观点来对数据和信息建模，也就是说，把现实世界中的客观对象
- 抽象为某一种信息结构，这种信息结构不依赖于具体的计算机系统，也不对应某个具体的

:::key
1. 信息模型，它是按用户的观点来对数据和信息建模，也就是说，把现实世界中的客观对象
2. DBMS，它是概念级别的模型。(23上案例)
3. 1.目前主要的数据结构有层次模型、网状模型、关系模型、面向对象模型和对象关系模型。
:::

:::warning
本卡片由 OCR 文本自动整理，`review_status` 为 `ai_draft`，上线前需要人工核对原文、表格和红色重点。
:::
