### 概述

==软件架构分为== 是本章需要掌握的考点之一。1、软件架构分为：①数据流风格：②调用/返回风格；③独立构件风格；④虚拟机风格：⑤仓库风格。

### 结构化要点

- 2、软件架构评估方式，分别是基于调查问卷(或检查表)的方式、基于场量的方式和基于度量的方式。
- 3、软件需求包括业务需求、用户需求和系统需求。
- 4、质量功能部署QFD将软件需求分三类，分别是常规需求、期望需求和意外需求。
- 5、需求过程主要包括需求获取、需求分析、需求规格说明书编制、需求验证与确认等。
- 6、使用结构化分析 SA方法进行需求分析，其建立的模型的核心是数据字典。围绕这个核心，有三个层
- 次的模型，分别是数据模型、功能模型和行模型(也称为状态模型)。使用实体关系图(E-R 图)表示数据模
- 型，用数据流图(DFD)表示功能模型，用态转换图(STD)表示行模型。

:::key
1. 1、软件架构分为：①数据流风格：②调用/返回风格；③独立构件风格；④虚拟机风格：⑤仓库风格。
2. 2、软件架构评估方式，分别是基于调查问卷(或检查表)的方式、基于场量的方式和基于度量的方式。
3. 3、软件需求包括业务需求、用户需求和系统需求。
:::

:::warning
本卡片由 OCR 文本自动整理，`review_status` 为 `ai_draft`，上线前需要人工核对原文、表格和红色重点。
:::
