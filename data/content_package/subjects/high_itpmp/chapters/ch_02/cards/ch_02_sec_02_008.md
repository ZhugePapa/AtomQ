### 概述

==常见的数据结构模型有三种== 是本章需要掌握的考点之一。3.常见的数据结构模型有三种：层次模型、网状模型和关系模型，层次模型和网状模

### 结构化要点

- 型又统称为格式化数据模型。
- 结构模型
- 层次模型
- 特点
- 用"树"结构表示实体集之间的关联，其中实体集(用矩形框表示)力
- 结点，而树中各结点之间的连线表示它们之间的关联。缺点并不是所
- 有事物都是层次关系

:::key
1. 结点，而树中各结点之间的连线表示它们之间的关联。缺点并不是所
2. 有事物都是层次关系
3. 存储单位。记录包含若干数据项。网状数据库是导航式数据库，用户
:::

:::warning
本卡片由 OCR 文本自动整理，`review_status` 为 `ai_draft`，上线前需要人工核对原文、表格和红色重点。
:::
