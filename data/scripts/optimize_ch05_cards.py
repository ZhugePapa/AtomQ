#!/usr/bin/env python3
"""Curate chapter 5 knowledge cards from the 2026 tricolor notes."""

from __future__ import annotations

import hashlib
import json
from collections import Counter
from datetime import datetime, timedelta, timezone
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
PUBLIC_ROOT = ROOT / "content_package" / "public"
SUBJECT_ID = "high_itpmp"
CHAPTER_ID = "ch_05"
CHAPTER_ROOT = PUBLIC_ROOT / "subjects" / SUBJECT_ID / "chapters" / CHAPTER_ID
CARDS_DIR = CHAPTER_ROOT / "cards"
BUILD_TIME = datetime.now(timezone(timedelta(hours=8))).isoformat(timespec="seconds")
CONTENT_VERSION = "2026.05.full-color-ocr-draft.10.ch05-curated"


SECTION_DESCRIPTIONS = {
    "sec_01": "软件架构风格、架构评估、敏感点与权衡点。",
    "sec_02": "QFD、需求开发、SA、UML、OOA 与对象关系。",
    "sec_03": "结构化设计、面向对象设计、设计原则与设计模式。",
    "sec_04": "配置管理、编码、软件测试分类与方法。",
    "sec_05": "部署交付、持续交付、持续部署、软件过程能力成熟度。",
    "sec_06": "概念模型、逻辑模型、物理模型和数据建模过程。",
    "sec_07": "元数据、数据元、数据模式、分类编码与标准化管理。",
    "sec_08": "数据存储、备份、容灾、数据质量和数据清理。",
    "sec_09": "数据集成、数据挖掘、数据服务、可视化和信息检索。",
    "sec_10": "数据库安全目标、对策与常用安全机制。",
    "sec_11": "网络传输、交换与系统集成基本原则。",
    "sec_12": "数据集成层次、异构集成方式与数据交换格式。",
    "sec_13": "CORBA、COM、DCOM、COM+、.NET、J2EE 等构件标准。",
    "sec_14": "应用集成、互操作性、可移植性、透明性和协调组件。",
    "sec_15": "信息安全三维空间、安全机制、安全服务与 ISSE-CMM。",
}


def md(text: str) -> str:
    return text.strip() + "\n"


def C(
    section_id: str,
    title: str,
    card_type: str,
    difficulty: int,
    page: str,
    tags: list[str],
    key_points: str,
    mnemonics: str,
    body: str,
) -> dict:
    return {
        "section_id": section_id,
        "title": title,
        "card_type": card_type,
        "difficulty": difficulty,
        "page": page,
        "tags": tags,
        "key_points": key_points.strip(),
        "mnemonics": mnemonics.strip(),
        "md": md(body),
    }


CARDS = [
    C("sec_01", "软件架构复用目标", "concept", 1, "48", ["软件工程", "架构设计", "软件复用"], "- 软件架构设计的核心问题：能否达到**架构级的软件复用**。", "", """
### 概述

软件架构不是只画结构图，它要在较高层次组织系统构件、关系和约束，使后续设计与实现可以复用稳定的架构经验。

### 核心内容

软件架构设计的一个核心问题，是系统能否达到==架构级的软件复用==。

### 易混点

复用可以发生在代码、组件、设计模式、框架等层面；这里强调的是更高层的==架构级==复用。
"""),
    C("sec_01", "通用软件架构风格", "formula", 2, "48", ["软件工程", "架构风格", "架构设计"], "- 五类通用风格：**数据流、调用/返回、独立构件、虚拟机、仓库**。", "记作：**数调独虚仓**。", """
### 概述

通用软件架构风格用于描述系统构件如何组织、通信和协作，是架构设计和架构评估中的高频基础考点。

### 核心内容

| 架构风格 | 常见形式 |
|:---|:---|
| ==数据流风格== | 批处理序列、管道/过滤器 |
| ==调用/返回风格== | 主程序/子程序、数据抽象和面向对象、层次结构 |
| ==独立构件风格== | 进程通信、事件驱动系统 |
| ==虚拟机风格== | 解释器、基于规则的系统 |
| ==仓库风格== | 数据库系统、黑板系统、超文本系统 |

### 易混点

管道/过滤器属于数据流风格；解释器属于虚拟机风格；黑板系统属于仓库风格。
"""),
    C("sec_01", "架构评估关注质量属性", "concept", 1, "48", ["软件工程", "架构评估", "质量属性"], "- 架构评估关注的是系统的**质量属性**，不是单个功能是否实现。", "", """
### 概述

架构评估通常发生在高层设计阶段，目标是判断架构是否能支撑系统的关键质量要求。

### 核心内容

在架构评估过程中，评估人员关注的是系统的==质量属性==，例如性能、可靠性、安全性、可维护性、可扩展性等。

### 易混点

功能测试关注“能不能做”；架构评估更关注“这样组织系统后，质量目标能不能达成”。
"""),
    C("sec_01", "敏感点与权衡点", "comparison", 2, "48", ["软件工程", "架构评估", "质量属性"], "- **敏感点**影响一个或多个质量属性。\n- **权衡点**同时影响多个质量属性，是多个质量属性的敏感点。", "", """
### 概述

敏感点和权衡点用于分析架构设计决策对质量属性的影响。

### 核心内容

| 概念 | 判断方式 |
|:---|:---|
| ==敏感点== | 一个或多个构件、构件关系的特性，会影响某个质量属性 |
| ==权衡点== | 影响多个质量属性的特性，是多个质量属性共同的敏感点 |

### 易混点

题干出现“同时影响多个质量属性”“需要在多个质量目标之间取舍”，优先判断为==权衡点==。
"""),
    C("sec_01", "架构评估方式与场景描述", "formula", 2, "48", ["软件工程", "架构评估", "ATAM"], "- 三类评估方式：**问卷/检查表、场景、度量**。\n- 场景描述三要素：**刺激、环境、响应**。\n- 场景法包括 **ATAM、SAAM、CBAM**。", "评估方式记：**问场度**；场景三要素记：**刺环响**。", """
### 概述

软件架构评估可通过检查表、场景或度量开展，其中基于场景的方法最适合考查质量属性与架构响应。

### 核心内容

| 项目 | 内容 |
|:---|:---|
| 主要评估方式 | ==基于调查问卷或检查表==、==基于场景==、==基于度量== |
| 场景法 | ==ATAM==、==SAAM==、==CBAM== |
| 场景描述 | ==刺激==、==环境==、==响应== |

### 易混点

场景中的“刺激”是干系人与系统交互的触发；“响应”是系统通过架构对刺激作出的反应。
"""),
    C("sec_02", "QFD 三类需求", "formula", 2, "48", ["软件工程", "需求分析", "QFD"], "- QFD 作用：把**用户要求转化为软件需求**。\n- 三类需求：**常规需求、期望需求、意外需求**。", "记作：**常期意**。", """
### 概述

质量功能部署 QFD 是一种把用户要求转化为软件需求的技术，目标是提升软件工程过程中的用户满意度。

### 核心内容

QFD 将软件需求分为三类：==常规需求==、==期望需求==和==意外需求==。

### 易混点

QFD 的关键词是“用户要求 -> 软件需求”；不要把它误认为测试方法或架构评估方法。
"""),
    C("sec_02", "需求开发四项活动", "process", 2, "48,49", ["软件工程", "需求分析", "SRS"], "- 需求开发主线：**需求获取 -> 需求分析 -> SRS 编制 -> 需求验证与确认**。\n- 验证确认常用：**需求评审、需求测试**。", "记作：**获析规验**。", """
### 概述

需求开发不是只收集用户说法，而是要把需求获取、分析、规格说明和验证确认串成闭环。

### 核心内容

| 活动 | 关注点 |
|:---|:---|
| ==需求获取== | 与用户有效合作，常见方法包括访谈、问卷调查等 |
| ==需求分析== | 提炼、分析、审查已获取需求，发现错误、遗漏和不足 |
| ==需求规格说明书（SRS）== | 让干系人与开发团队对系统初始规定形成共同理解 |
| ==需求验证与确认== | 通过需求评审、需求测试及早发现问题 |

### 易混点

SRS 是需求开发活动的产物；需求评审和需求测试用于验证与确认需求。
"""),
    C("sec_02", "面向对象分析模型", "concept", 2, "49", ["软件工程", "OOA", "用例模型"], "- OOA 核心工作：建立**用例模型**与**分析模型**。\n- 用例模型偏需求，分析模型偏系统逻辑结构。", "", """
### 概述

面向对象分析 OOA 的目标，是用对象、类和它们之间的关系理解系统需求与基本逻辑结构。

### 核心内容

OOA 阶段的核心工作是建立==用例模型==与==分析模型==。

- 用例模型：用用例描述系统需求。
- 分析模型：描述系统基本逻辑结构，包括对象和类如何组成系统，以及它们如何通信以实现系统行为。

### 易混点

用例模型不是界面原型；它描述系统与参与者之间的目标和交互。
"""),
    C("sec_02", "SA 模型核心与三层模型", "formula", 2, "49", ["软件工程", "结构化分析", "数据字典"], "- SA 核心：**数据字典**。\n- 三层模型：**数据模型、功能模型、行为/状态模型**。", "围绕数据字典记：**数、功、行**。", """
### 概述

结构化分析 SA 关注数据在系统中的流动、加工和状态变化。

### 核心内容

使用 SA 方法进行需求分析时，模型核心是==数据字典==。围绕这个核心，有三个层次的模型：

| 模型 | 常用图形 |
|:---|:---|
| ==数据模型== | 实体联系图 E-R 图 |
| ==功能模型== | 数据流图 DFD |
| ==行为模型/状态模型== | 状态转换图 STD |

### 易混点

DFD 表示功能模型，不表示数据模型；E-R 图表示数据模型。
"""),
    C("sec_02", "UML 结构与建模元素", "formula", 2, "49", ["软件工程", "UML", "建模元素"], "- UML 结构三部分：**构造块、规则、公共机制**。\n- 事物四类：**结构、行为、分组、注释**。", "UML 结构记：**构规公**；事物记：**结行分注**。", """
### 概述

UML 是统一建模语言，用于从不同角度描述系统结构、行为、组织和说明信息。

### 核心内容

| 考点 | 枚举 |
|:---|:---|
| UML 结构 | ==构造块==、==规则==、==公共机制== |
| UML 事物/建模元素 | ==结构事物==、==行为事物==、==分组事物==、==注释事物== |

### 易混点

“事物”也称建模元素；“结构、行为、分组、注释”是 UML 的建模元素分类。
"""),
    C("sec_02", "UML 四类关系", "comparison", 2, "49", ["软件工程", "UML", "关系"], "- UML 关系：**依赖、关联、泛化、实现**。\n- 组合、聚合属于关联中的整体-部分关系。", "记作：**依关泛实**。", """
### 概述

UML 关系描述模型元素之间的语义联系，是类图和用例图等图形建模的基础。

### 核心内容

| 关系 | 判断关键词 |
|:---|:---|
| ==依赖== | 一个事物变化会影响另一个事物 |
| ==关联== | 一组对象之间连接的结构关系 |
| ==泛化== | 一般/特殊关系，父类与子类关系 |
| ==实现== | 一个类元保证执行另一个类元指定的契约 |

### 易混点

组合和聚合都是整体-部分关系，但组合更强，通常生命周期一致。
"""),
    C("sec_02", "常用 UML 图用途", "comparison", 3, "49,50", ["软件工程", "UML", "UML图"], "- 重点按用途识别：**需求看用例图，静态结构看类图/对象图，交互看顺序图/通信图，流程看活动图，部署看部署图**。", "", """
### 概述

UML 图不建议死背长清单，考试更常按“图的用途”考判断。

### 核心内容

| 图 | 主要用途 |
|:---|:---|
| ==用例图== | 描述用例、参与者及其关系 |
| ==类图== | 展现类、接口、协作及关系，常用于静态设计视图 |
| ==对象图== | 描述类图中事物实例的静态快照 |
| ==顺序图== | 强调消息的时间顺序 |
| ==通信图== | 强调收发消息对象的组织结构 |
| ==状态图== | 描述状态、转移、事件和活动 |
| ==活动图== | 描述控制流和数据流，适合业务流程建模 |
| ==部署图== | 描述运行时处理节点及构件部署 |

### 易混点

顺序图强调“时间顺序”，通信图强调“对象组织结构”。
"""),
    C("sec_02", "UML 五个系统视图", "formula", 2, "50", ["软件工程", "UML", "系统视图"], "- 五视图：**逻辑视图、实现视图、部署视图、用例视图、进程视图**。\n- 用例视图是最基本的需求分析模型。", "可按“**逻实部用进**”记忆。", """
### 概述

UML 系统视图从不同角度组织模型，帮助把需求、设计、实现、部署和运行并发结构联系起来。

### 核心内容

| 系统视图 | 关注点 |
|:---|:---|
| ==逻辑视图== | 设计模型中架构上重要的类、子系统和用例实现 |
| ==实现视图== | 物理代码文件和构件 |
| ==部署视图== | 软件到硬件节点的映射与分布结构 |
| ==用例视图== | 最基本的需求分析模型 |
| ==进程视图== | 可执行线程和进程，描述并发与同步结构 |

### 易混点

部署视图关注软件到硬件的映射；实现视图关注代码文件和构件。
"""),
    C("sec_02", "用例建模四阶段", "process", 2, "50", ["软件工程", "OOA", "用例建模"], "- 用例模型四阶段：**识别参与者、合并需求获得用例、细化用例描述、调整用例模型**。\n- 前三阶段是必须的。", "记作：**识、合、细、调**。", """
### 概述

用例建模是用用例描述系统需求的过程，核心是把参与者目标整理为清晰的系统交互。

### 核心内容

构建用例模型一般经历四个阶段：==识别参与者==、==合并需求获得用例==、==细化用例描述==和==调整用例模型==。

其中前三个阶段是必须的。

### 易混点

“参与者”不一定是人，也可以是与系统交互的外部系统或组织角色。
"""),
    C("sec_02", "用例之间三类关系", "comparison", 2, "50", ["软件工程", "OOA", "用例关系"], "- 用例关系：**包含、扩展、泛化**。\n- 公共行为用包含；可选/分支行为用扩展；共性抽象用泛化。", "记作：**包扩泛**。", """
### 概述

用例之间的关系用于复用公共行为、表达可选扩展或抽象共性。

### 核心内容

| 关系 | 判断方式 |
|:---|:---|
| ==包含== | 多个用例可抽取公共行为 |
| ==扩展== | 基本用例在特定条件下出现额外分支 |
| ==泛化== | 多个用例具有类似结构和行为，可抽象为父用例 |

### 易混点

“包含”偏公共必经行为；“扩展”偏条件触发的额外行为。
"""),
    C("sec_02", "类之间六类关系", "comparison", 2, "50", ["软件工程", "OOA", "类关系"], "- 类关系：**关联、依赖、泛化、聚合、组合、实现**。\n- 聚合弱，组合强；组合生命周期一致。", "记作：**关依泛聚组实**。", """
### 概述

类之间的关系用于描述对象结构、依赖变化、继承层次、整体部分和接口实现。

### 核心内容

| 关系 | 判断关键词 |
|:---|:---|
| ==关联== | 对象实例之间的结构关系 |
| ==依赖== | B 的变化可能引起 A 的变化 |
| ==泛化== | 父类与子类的一般/特殊关系 |
| ==聚合== | 弱整体-部分，部分可属于多个整体，生命周期可不同 |
| ==组合== | 强整体-部分，部分只属于一个整体，生命周期相同 |
| ==实现== | 类实现接口中声明的行为 |

### 易混点

汽车和车轮常用于聚合；公司和部门常用于组合。
"""),
    C("sec_03", "结构化设计 SD", "concept", 2, "50", ["软件工程", "软件设计", "结构化设计"], "- SD 是**面向数据流**的方法。\n- 基础文档：**SRS、DFD、数据字典**。\n- 过程：**自顶向下、逐步求精、模块化**。", "", """
### 概述

结构化设计 SD 以结构化分析阶段的成果为基础，把软件设计为相对独立、功能单一的模块结构。

### 核心内容

SD 是一种==面向数据流==的方法，以 SRS、DFD 和数据字典等文档为基础，是一个==自顶向下==、==逐步求精==和==模块化==的过程。

SD 通常分为概要设计和详细设计两个阶段。

### 易混点

SA 是需求分析方法；SD 是软件设计方法。两者都与数据流和数据字典相关，但阶段不同。
"""),
    C("sec_03", "高内聚低耦合", "formula", 1, "50", ["软件工程", "软件设计", "内聚耦合"], "- 设计基本原则：**高内聚、低耦合**。\n- 内聚看模块内部，耦合看模块之间。", "记作：**内高外低**。", """
### 概述

模块化设计追求模块内部职责集中、模块之间依赖尽量少。

### 核心内容

结构化设计需要遵循的基本原则是==高内聚==、==低耦合==。

- 内聚：模块内部各成分之间的联系程度。
- 耦合：模块之间联系的程度。

### 易混点

“高内聚”不是模块越大越好，而是模块内部职责越集中越好。
"""),
    C("sec_03", "OOD 基本思想", "concept", 1, "50", ["软件工程", "OOD", "面向对象设计"], "- OOD 是 OOA 的延续。\n- 基本思想：**抽象、封装、可扩展性**。\n- 可扩展性主要通过**继承和多态**实现。", "", """
### 概述

面向对象设计 OOD 是面向对象分析 OOA 的延续，把分析模型进一步转化为可实现的软件设计。

### 核心内容

OOD 的基本思想包括==抽象==、==封装==和==可扩展性==。其中，可扩展性主要通过==继承==和==多态==实现。

### 易混点

OOA 偏“分析问题域”，OOD 偏“设计解决方案”。
"""),
    C("sec_03", "OOD 七项原则", "formula", 3, "50", ["软件工程", "OOD", "设计原则"], "- 七原则：**单一职责、开闭、李氏替换、依赖倒置、接口隔离、组合重用、迪米特**。", "可记：**单开李依接组迪**。", """
### 概述

面向对象设计原则用于控制类职责、扩展方式、依赖方向和对象之间的知识范围。

### 核心内容

| 原则 | 核心判断 |
|:---|:---|
| ==单一职责原则== | 一个类只承担单一功能 |
| ==开闭原则== | 对扩展开放，对修改封闭 |
| ==李氏替换原则== | 子类可以替换父类 |
| ==依赖倒置原则== | 依赖抽象，不依赖具体实现 |
| ==接口隔离原则== | 多个专门接口优于单一总接口 |
| ==组合重用原则== | 尽量用组合而不是继承复用 |
| ==迪米特原则== | 对其他对象了解尽可能少 |

### 易混点

迪米特原则与低耦合思想一致；单一职责原则与高内聚思想一致。
"""),
    C("sec_03", "设计模式分类", "formula", 2, "51", ["软件工程", "设计模式", "软件设计"], "- 按范围：**类模式、对象模式**。\n- 按目的：**创建型、结构型、行为型**。", "目的分类记：**创结行**。", """
### 概述

设计模式是前人经验的总结，用于复用成功的软件设计方案。

### 核心内容

| 分类维度 | 类型 |
|:---|:---|
| 处理范围 | ==类模式==、==对象模式== |
| 目的用途 | ==创建型模式==、==结构型模式==、==行为型模式== |

### 易混点

类模式通过继承建立关系，编译时确定，偏静态；对象模式处理运行时变化的对象关系，偏动态。
"""),
    C("sec_03", "三类设计模式清单", "formula", 3, "51", ["软件工程", "设计模式", "创建型结构型行为型"], "- 创建型管**创建对象**；结构型管**类或对象组合**；行为型管**交互和职责分配**。", "", """
### 概述

设计模式清单较长，学习时先按“目的”归类，再记典型模式。

### 核心内容

| 类型 | 用途 | 常见模式 |
|:---|:---|:---|
| ==创建型== | 创建对象 | 工厂方法、抽象工厂、原型、单例、建造者 |
| ==结构型== | 处理类或对象组合 | 适配器、桥接、组合、装饰、外观、享元、代理 |
| ==行为型== | 描述交互与职责分配 | 职责链、命令、解释器、迭代器、中介者、备忘录、观察者、状态、策略、模板方法、访问者 |

### 易混点

“工厂”“单例”“建造者”都属于创建型；“适配器”“装饰”“代理”属于结构型；“观察者”“策略”“状态”属于行为型。
"""),
    C("sec_04", "软件配置管理活动", "formula", 2, "51", ["软件工程", "配置管理", "软件实现"], "- 配置管理活动：**计划、标识、控制、状态记录、审计、发布管理与交付**。", "记作：**计标控状审发**。", """
### 概述

软件配置管理用于识别、控制和审计软件生命周期中的配置项及其变更。

### 核心内容

软件配置管理活动包括==软件配置管理计划==、==软件配置标识==、==软件配置控制==、==软件配置状态记录==、==软件配置审计==、==软件发布管理与交付==。

### 易混点

配置控制管变更；配置审计评价产品和过程是否遵从规则、标准、指南、计划和流程。
"""),
    C("sec_04", "软件编码定义", "concept", 1, "51", ["软件工程", "编码", "软件实现"], "", "", """
### 概述

编码是把软件设计结果转化为计算机可执行或可解释形式的活动。

### 核心内容

软件编码就是把软件设计的结果翻译成计算机可以==理解和识别==的形式，即用某种程序设计语言书写程序。

### 易混点

编码不是需求分析，也不是测试；它承接软件设计，产出程序代码。
"""),
    C("sec_04", "软件测试目的", "concept", 1, "51", ["软件工程", "软件测试", "质量"], "- 软件测试目的：验证质量要求、发现缺陷、为质量测量和评价提供依据。", "", """
### 概述

软件测试通过执行或检查软件产品，判断其是否满足规定的软件质量要求。

### 核心内容

软件测试的目的包括：验证软件是否满足合同、计划、设计文档、SRS 和产品说明等规定的质量要求；通过测试发现==软件缺陷==；为软件产品质量测量和评价提供依据。

### 易混点

测试不能证明软件没有缺陷，只能在一定范围内发现缺陷并提供质量评价依据。
"""),
    C("sec_04", "静态测试与白盒黑盒", "comparison", 3, "51,52", ["软件工程", "软件测试", "白盒黑盒"], "- 静态测试：**桌前检查、代码走查、代码审查**。\n- 白盒看内部结构，常用于**单元测试**。\n- 黑盒不看内部结构，常用于**集成、确认、系统测试**。", "", """
### 概述

软件测试常按是否运行程序、是否关注内部结构来分类。

### 核心内容

| 类型 | 关注点 | 常见方法 |
|:---|:---|:---|
| ==静态测试== | 不运行程序，检查文档或代码 | 桌前检查、代码走查、代码审查 |
| ==白盒测试== | 考虑程序内部结构和处理算法 | 控制流测试、数据流测试、程序变异测试、逻辑覆盖 |
| ==黑盒测试== | 不考虑或不了解内部结构 | 等价类划分、边界值分析、判定表、因果图、状态图、猜错法、正交试验法 |

### 易混点

白盒常见覆盖标准包括语句覆盖、判定覆盖、条件覆盖、条件/判定覆盖、条件组合覆盖、修正条件/判定覆盖和路径覆盖。
"""),
    C("sec_05", "部署交付活动", "concept", 1, "52", ["软件工程", "部署交付", "软件部署"], "- 部署交付活动：**打包、安装、配置、测试、集成、更新**。", "", """
### 概述

软件开发完成后，需要部署到最终用户的正式运行环境，并交付给最终用户使用。

### 核心内容

部署交付活动包括软件==打包==、==安装==、==配置==、==测试==、==集成==和==更新==等，是一个持续不断的过程。

### 易混点

部署交付不是一次性复制文件，它还包括配置、集成、测试和后续更新。
"""),
    C("sec_05", "持续交付三阶段改进", "process", 2, "52", ["软件工程", "持续交付", "DevOps"], "- 持续交付改进：需求阶段用**用户故事**；开发测试阶段做**持续集成**；运维阶段打通**开发与运维**。", "", """
### 概述

持续交付提供一套改进传统软件开发流程的方案，使软件在整个生命周期中保持可部署状态。

### 核心内容

| 阶段 | 改进点 |
|:---|:---|
| ==需求阶段== | 使用便于开发人员理解的用户故事 |
| ==开发测试阶段== | 做到持续集成，让测试人员尽早进入项目 |
| ==运维阶段== | 打通开发和运维通路，保持开发环境和运维环境统一 |

### 易混点

持续交付强调“可部署”；持续部署更进一步，强调自动部署到环境。
"""),
    C("sec_05", "持续交付优势", "formula", 2, "52", ["软件工程", "持续交付", "DevOps"], "- 持续交付优势：**缩短提交到部署时间、降低风险、快速反馈、保持可部署、简化部署、过程可视化**。", "", """
### 概述

持续交付的价值在于让交付过程更可靠、更可预期，并缩短从代码提交到正式部署的时间。

### 核心内容

持续交付的优势包括：==缩短提交代码到正式部署的时间==、==降低部署风险==、==自动快速反馈==、==让软件始终处于可部署状态==、==简化部署步骤==、==让交付过程可靠可预期可视化==。

### 易混点

持续交付不等于每次提交都自动上线；它强调软件随时具备部署能力。
"""),
    C("sec_05", "镜像部署 Build-Ship-Run", "process", 2, "52", ["软件工程", "持续部署", "镜像部署"], "- 镜像部署三个环节：**Build、Ship、Run**。\n- 容器部署常见方案：**Kubernetes + Docker、Matrix**。", "记作：**构建、运送、运行**。", """
### 概述

容器技术是持续部署中的常用技术，完整镜像部署通常分为 Build、Ship、Run 三个环节。

### 核心内容

| 环节 | 含义 |
|:---|:---|
| ==Build== | 将软件编译形成 RPM 包或 Jar 包等制品 |
| ==Ship== | 安装所需第三方依赖和插件到环境中 |
| ==Run== | 在目标环境启动整套运行环境 |

### 易混点

Build-Ship-Run 是镜像部署环节，不是软件生命周期阶段。
"""),
    C("sec_05", "蓝绿部署与金丝雀部署", "comparison", 2, "53", ["软件工程", "部署方式", "蓝绿部署"], "- 蓝绿部署：**新旧两套环境，切 DNS，易回滚**。\n- 金丝雀部署：**少量用户先试用，观察后逐步放量**。", "", """
### 概述

蓝绿部署和金丝雀部署都是降低发布风险的部署方式。

### 核心内容

| 部署方式 | 判断关键词 |
|:---|:---|
| ==蓝绿部署== | 准备新旧两个版本，通过域名解析切换用户环境，问题时快速切回旧版本 |
| ==金丝雀部署== | 新版本先给少量用户使用，观察无问题后逐步扩大范围 |

### 易混点

蓝绿部署强调“两套环境切换”；金丝雀部署强调“小流量试运行、逐步放量”。
"""),
    C("sec_05", "不可变服务器", "concept", 1, "53", ["软件工程", "部署方式", "不可变服务器"], "- 不可变服务器：除**更新和安装补丁**外，不对服务器做任何更改。", "", """
### 概述

不可变服务器是一种部署模式，用来降低环境漂移带来的发布和运维风险。

### 核心内容

不可变服务器是指除了==更新和安装补丁程序==以外，不对服务器进行任何更改。

### 易混点

不可变服务器不是永远不更新，而是避免在运行环境中随意手工修改配置和依赖。
"""),
    C("sec_05", "软件过程能力与 CSMM", "formula", 3, "53", ["软件工程", "软件过程能力", "CSMM"], "- 软件过程能力包括：**治理、开发与交付、管理与支持、组织管理**。\n- CSMM：**4 个能力域、20 个能力子域、161 个能力要求**。", "", """
### 概述

软件过程能力是组织基于过程、技术、资源和人员能力达成业务目标的综合能力。

### 核心内容

软件过程能力包括==治理能力==、==开发与交付能力==、==管理与支持能力==、==组织管理能力==等方面。

CSMM 模型由==4 个能力域==、==20 个能力子域==、==161 个能力要求==组成。

### 易混点

CSMM 是软件过程能力成熟度相关模型，不要与后续 CMMI、DCMM、ISSE-CMM 混淆。
"""),
    C("sec_05", "CSMM 五级成熟度", "formula", 2, "53", ["软件工程", "CSMM", "成熟度"], "- 五级：**初始级、项目规范级、组织改进级、量化提升级、创新引领级**。", "记作：**初、项、组、量、创**。", """
### 概述

CSMM 用成熟度等级描述组织软件过程能力从初始到创新引领的演进。

### 核心内容

CSMM 成熟度等级包括：==1级初始级==、==2级项目规范级==、==3级组织改进级==、==4级量化提升级==、==5级创新引领级==。

### 易混点

看到“量化”通常对应第 4 级；看到“创新引领”对应第 5 级。
"""),
    C("sec_06", "数据模型三类", "formula", 1, "53", ["数据工程", "数据建模", "数据模型"], "- 按应用目的：**概念模型、逻辑模型、物理模型**。", "记作：**概逻物**。", """
### 概述

数据建模按模型应用目的可以分为概念、逻辑和物理三个层次。

### 核心内容

根据模型应用目的不同，数据模型可划分为==概念模型==、==逻辑模型==和==物理模型==。

### 易混点

概念模型偏用户视角；逻辑模型偏数据结构；物理模型偏数据库实现。
"""),
    C("sec_06", "概念模型逻辑模型物理模型", "comparison", 2, "53,54", ["数据工程", "数据建模", "数据模型"], "- 关系模型是目前最重要的逻辑数据模型。\n- 关系完整性：**实体完整性、参照完整性、用户定义完整性**。", "", """
### 概述

三类数据模型从业务理解逐步走向数据库实现。

### 核心内容

| 模型 | 关注点 |
|:---|:---|
| ==概念模型== | 又称信息模型，从用户观点描述现实世界对象 |
| ==逻辑模型== | 描述数据结构，常见层次、网状、关系、面向对象、对象关系模型 |
| ==物理模型== | 基于逻辑模型，面向数据库架构和实现 |

关系模型的数据操作包括查询、插入、删除和更新；完整性包括==实体完整性==、==参照完整性==、==用户定义完整性==。

### 易混点

表、字段、视图、索引、存储过程和触发器更偏物理模型。
"""),
    C("sec_06", "数据建模过程", "process", 2, "54", ["数据工程", "数据建模", "建模过程"], "- 数据建模过程：**数据需求分析、概念模型设计、逻辑模型设计、物理模型设计**。", "记作：**需概逻物**。", """
### 概述

数据建模从需求开始，逐步抽象、结构化，再落到数据库实现。

### 核心内容

数据建模过程包括==数据需求分析==、==概念模型设计==、==逻辑模型设计==和==物理模型设计==。

### 易混点

不要把物理模型设计提前到需求阶段；物理模型是在逻辑模型基础上考虑数据库实现。
"""),
    C("sec_06", "物理模型常见对象", "formula", 1, "54", ["数据工程", "数据建模", "物理模型"], "- 物理模型常见对象：**表、字段、视图、索引、存储过程、触发器**。", "", """
### 概述

物理模型是数据库实现层面的模型，关注数据如何在具体数据库中组织和访问。

### 核心内容

物理模型常见元素包括==表==、==字段==、==视图==、==索引==、==存储过程==和==触发器==。

### 易混点

为了性能进行反规范化属于物理模型层面的常见考虑。
"""),
    C("sec_07", "数据标准化主要内容", "formula", 2, "54", ["数据工程", "数据标准化", "标准化内容"], "- 主要内容：**元数据、数据元、数据模式、数据分类与编码、数据标准化管理**。", "记作：**元、元、模、分、管**。", """
### 概述

数据标准化是数据共享和数据互操作的基础，目标是让数据简单化、结构化和规范化。

### 核心内容

数据标准化主要包括==元数据标准化==、==数据元标准化==、==数据模式标准化==、==数据分类与编码标准化==和==数据标准化管理==。

### 易混点

元数据和数据元不是一回事：元数据是“描述数据的数据”，数据元是数据表示与交换的基本单位。
"""),
    C("sec_07", "元数据标准化", "concept", 2, "54", ["数据工程", "数据标准化", "元数据"], "- 元数据：**描述数据的数据**。\n- 描述信息资源或数据的内容、范围、质量、管理、所有者、提供方式等。", "", """
### 概述

元数据用于描述信息资源或数据，让数据更容易被发现、理解、管理和使用。

### 核心内容

元数据是==描述数据的数据==，是信息资源或数据结构化描述的信息，通常包括内容、范围、质量、管理方式、数据所有者和数据提供方式等。

### 易混点

元数据不是业务数据本身，而是对业务数据的说明。
"""),
    C("sec_07", "数据元组成与抽取", "concept", 2, "54", ["数据工程", "数据标准化", "数据元"], "- 数据元组成：**对象、特性、表示**。\n- 抽取方式：**自上而下、自下而上**；新系统一般自上而下。", "", """
### 概述

数据元是数据库、文件和数据交换中的基本数据单元，是标准化管理的重要对象。

### 核心内容

数据元由==对象==、==特性==和==表示==三部分组成。数据元提取方式包括==自上而下==和==自下而上==，新建系统一般采用自上而下方式。

### 易混点

数据元强调“基本单位”；元数据强调“描述数据的数据”。
"""),
    C("sec_07", "数据元标准化过程", "process", 3, "54", ["数据工程", "数据标准化", "数据元"], "- 数据元标准化：**定义业务范围 -> 业务过程分析与建模 -> 提取并规范属性 -> 编制值域/代码表 -> 标准协调 -> 发布维护**。", "", """
### 概述

数据元标准化需要从业务范围出发，经过建模、提取、规范、协调和维护，形成可复用的数据标准。

### 核心内容

1. ==描述和定义业务范围==。
2. ==业务过程分析和信息建模==。
3. 提取数据元并规范属性。
4. 对代码类数据元编制值域和代码表。
5. 与国家、行业或相关标准协调。
6. 发布并动态维护。

### 易混点

不要把“发布标准”当成最后一劳永逸；数据标准需要动态维护。
"""),
    C("sec_07", "数据模式与分类编码标准化", "comparison", 2, "54,55", ["数据工程", "数据标准化", "数据模式"], "- 数据模式标准化减少**歧义和冗余**。\n- 分类与编码标准化用于统一分类体系和代码表达。", "", """
### 概述

数据模式标准化和分类编码标准化都服务于数据共享、交换和统一理解。

### 核心内容

| 标准化对象 | 作用 |
|:---|:---|
| ==数据模式标准化== | 建立统一数据模型，减少数据歧义和冗余 |
| ==数据分类与编码标准化== | 统一数据分类体系和编码规则，便于统计、交换和检索 |

### 易混点

数据字典常用于支撑数据模式标准化；分类编码重点是“分类体系”和“代码表达”。
"""),
    C("sec_07", "数据标准化管理阶段", "process", 2, "55", ["数据工程", "数据标准化", "标准化管理"], "- 阶段：**确定数据需求、制定数据标准、批准数据标准、实施数据标准**。", "记作：**需制批实**。", """
### 概述

数据标准化管理确保标准不是停留在文档，而是能被批准、执行和维护。

### 核心内容

数据标准化管理阶段包括==确定数据需求==、==制定数据标准==、==批准数据标准==和==实施数据标准==。

### 易混点

制定标准不是终点，必须经过批准并落到实施。
"""),
    C("sec_08", "数据存储与备份结构", "formula", 2, "55", ["数据工程", "数据运维", "数据备份"], "- 备份结构：**DAS、基于 LAN、LAN-FREE、SERVER-FREE**。", "", """
### 概述

数据存储要求安全、有效地保存数据并保证访问；数据备份用于防止误操作、系统故障等导致的数据丢失。

### 核心内容

常见数据备份结构包括==DAS 备份结构==、==基于 LAN 的备份结构==、==LAN-FREE 备份结构==和==SERVER-FREE 备份结构==。

### 易混点

备份结构关注数据如何被备份；备份策略关注每次备份什么内容。
"""),
    C("sec_08", "完全差异增量备份", "comparison", 2, "55", ["数据工程", "数据备份", "备份策略"], "- 三种备份策略：**完全备份、差异备份、增量备份**。\n- 完全备份最完整；增量备份只备上次备份后变化；差异备份备上次完全备份后变化。", "", """
### 概述

备份策略决定每次备份的数据范围，会影响备份时间、恢复速度和存储成本。

### 核心内容

| 策略 | 数据范围 |
|:---|:---|
| ==完全备份== | 备份全部数据 |
| ==差异备份== | 备份上一次完全备份以来变化的数据 |
| ==增量备份== | 备份上一次任意备份以来变化的数据 |

### 易混点

差异备份参照“上一次完全备份”；增量备份参照“上一次备份”。
"""),
    C("sec_08", "应用容灾与数据容灾", "comparison", 2, "55", ["数据工程", "容灾", "RPO"], "- 容灾分为**应用容灾**和**数据容灾**。\n- **RPO** 看允许丢多少数据，**RTO** 看恢复要多久。", "", """
### 概述

容灾系统按保护对象可分为应用容灾和数据容灾。数据备份是容灾体系中的最后防线。

### 核心内容

| 指标 | 含义 |
|:---|:---|
| ==RPO== | 恢复点目标，表示允许的数据丢失量 |
| ==RTO== | 恢复时间目标，表示业务恢复需要的时间 |

数据容灾是应用容灾的基础和组成部分。

### 易混点

RPO 关心“丢多少”，RTO 关心“多久恢复”。
"""),
    C("sec_08", "数据质量评价与控制", "formula", 2, "55", ["数据工程", "数据质量", "质量控制"], "- 评价方法：**直接评价法、间接评价法**。\n- 控制：**前期控制、后期控制**；建库流程还可分前期、过程、系统检测、准确性评价。", "", """
### 概述

数据质量管理通过评价和控制保障数据满足使用要求。

### 核心内容

数据质量评价方法分为==直接评价法==和==间接评价法==。数据质量控制可分为==前期控制==和==后期控制==。

按数据库建设流程，还可分为前期控制、过程控制、系统检测和准确性评价。

### 易混点

评价方法回答“怎么判断质量”；控制方法回答“怎么预防或纠正质量问题”。
"""),
    C("sec_08", "数据清理三步骤", "process", 1, "55", ["数据工程", "数据清理", "数据质量"], "- 数据清理：**数据分析、数据检测、数据修正**。", "记作：**析、检、修**。", """
### 概述

数据清理用于发现并修正数据中的错误、不一致和不完整问题。

### 核心内容

数据清理主要包括==数据分析==、==数据检测==和==数据修正==三个步骤。

### 易混点

数据清理是数据质量改进手段之一，不等同于数据备份或数据恢复。
"""),
    C("sec_09", "数据开发利用内容", "formula", 2, "56", ["数据工程", "数据开发利用", "数据服务"], "- 内容：**数据集成、数据挖掘、数据服务、可视化、信息检索**。", "记作：**集、挖、服、视、检**。", """
### 概述

数据开发利用把数据资源转化为可查询、可分析、可展示和可应用的价值。

### 核心内容

数据开发利用主要包括==数据集成==、==数据挖掘==、==数据服务==、==可视化==和==信息检索==。

### 易混点

数据标准化解决“统一规范”；数据开发利用解决“如何产生价值”。
"""),
    C("sec_09", "数据挖掘任务与流程", "process", 3, "56", ["数据工程", "数据挖掘", "挖掘流程"], "- 挖掘任务：**概念描述、关联分析、分类预测、聚类、孤立点分析**。\n- 流程：**确定对象、数据准备、数据挖掘、结果评价、结果应用**。", "", """
### 概述

数据挖掘从大量、不完全、有噪声、模糊、随机的数据中提取隐含的、有用的知识。

### 核心内容

| 考点 | 枚举 |
|:---|:---|
| 典型任务 | ==概念描述==、==关联分析==、==分类与预测==、==聚类==、==孤立点分析== |
| 一般流程 | ==确定分析对象==、==数据准备==、==数据挖掘==、==结果评价==、==结果应用== |

### 易混点

分类与预测通常有目标类别或变量；聚类强调无监督地发现相似分组。
"""),
    C("sec_09", "数据服务三类", "formula", 1, "56", ["数据工程", "数据服务", "数据开发利用"], "- 数据服务：**目录服务、查询/浏览/下载服务、分发服务**。", "记作：**目录、查下、分发**。", """
### 概述

数据服务把数据资源以可发现、可查询、可获取的方式提供给使用者。

### 核心内容

数据服务主要包括==数据目录服务==、==数据查询与浏览及下载服务==和==数据分发服务==。

### 易混点

数据目录服务偏“找得到”；查询浏览下载偏“取得到”；分发服务偏“推送或分发出去”。
"""),
    C("sec_09", "七类数据可视化", "formula", 2, "56", ["数据工程", "数据可视化", "可视化类型"], "- 七类：**一维、二维、三维、多维、时态、层次、网络**。", "记作：**一二三多时层网**。", """
### 概述

数据可视化通过图形化方式呈现数据特征、关系和变化。

### 核心内容

可视化表现方式可分为==一维数据可视化==、==二维数据可视化==、==三维数据可视化==、==多维数据可视化==、==时态数据可视化==、==层次数据可视化==和==网络数据可视化==。

### 易混点

时态可视化突出时间变化；网络可视化突出节点和关系。
"""),
    C("sec_09", "信息检索方法与技术", "formula", 2, "56", ["数据工程", "信息检索", "检索技术"], "- 方法：**全文、字段、基于内容的多媒体、数据挖掘**。\n- 技术：**布尔逻辑、截词、临近、限定字段、限制检索**。", "", """
### 概述

信息检索用于从信息集合中快速定位满足用户需求的信息。

### 核心内容

| 类型 | 枚举 |
|:---|:---|
| 检索方法 | ==全文检索==、==字段检索==、==基于内容的多媒体检索==、==数据挖掘== |
| 常用技术 | ==布尔逻辑检索==、==截词检索==、==临近检索==、==限定字段检索==、==限制检索== |

### 易混点

字段检索是按字段限定范围；限定字段检索技术是具体检索技巧，两者表述相近但语境不同。
"""),
    C("sec_10", "数据库安全对策", "formula", 2, "56", ["数据工程", "数据库安全", "安全对策"], "- 数据库安全目标包括：防非法访问、防推理、完整性、语义完整性、审计日志、身份鉴别、保密数据管理、多级保护等。", "", """
### 概述

数据库安全要保护数据库中的数据不被非法访问、泄露、篡改或推断。

### 核心内容

数据库安全对策包括==防止非法访问==、==防止推理==、==保证完整性==、==保证语义完整性==、==审计和日志==、==身份鉴别和认证==、==保密数据管理==、==多级保护==以及边界限制等。

### 易混点

“防推理”关注通过合法查询间接推断敏感信息，不等同于普通访问控制。
"""),
    C("sec_10", "数据库安全机制", "formula", 2, "56", ["数据工程", "数据库安全", "安全机制"], "- 常用机制：**用户标识和鉴别、访问控制、数据库加密、数据审计、推理控制**。", "记作：**鉴、控、密、审、推**。", """
### 概述

数据库安全机制是落实数据库安全目标的具体技术和管理手段。

### 核心内容

常用数据库安全机制包括==用户标识和鉴别==、==访问控制==、==数据库加密==、==数据审计==和==推理控制==。

### 易混点

访问控制解决“谁能访问什么”；数据库加密解决“即使获取数据也难以理解内容”。
"""),
    C("sec_11", "网络集成传输与交换", "formula", 2, "57", ["系统集成", "网络集成", "传输介质"], "- 无线介质：**无线电、微波、红外线**。\n- 有线介质：**双绞线、同轴电缆、光纤**。\n- 网络按覆盖范围：**局域网、城域网、广域网**。", "", """
### 概述

网络集成涉及传输子系统和交换子系统，是系统集成的基础设施层工作。

### 核心内容

| 子系统 | 重点 |
|:---|:---|
| ==传输子系统== | 无线介质包括无线电、微波、红外线；有线介质包括双绞线、同轴电缆、光纤 |
| ==交换子系统== | 网络按覆盖范围可分为局域网、城域网、广域网 |

### 易混点

“传输”关注介质和链路；“交换”关注网络中的连接和转发。
"""),
    C("sec_11", "系统集成基本原则", "formula", 1, "57", ["系统集成", "集成原则", "信息系统工程"], "- 系统集成技术原则：**开放性、结构化、先进性、主流化**。", "记作：**开结先主**。", """
### 概述

系统集成要在技术路线和实施方式上兼顾互通、扩展和可持续维护。

### 核心内容

系统集成工作在技术上需要遵循的基本原则包括==开放性==、==结构化==、==先进性==和==主流化==。

### 易混点

开放性强调接口和标准；主流化强调采用成熟主流技术路线。
"""),
    C("sec_12", "数据集成四个层次", "formula", 2, "57", ["系统集成", "数据集成", "集成层次"], "- 四层次：**基本数据集成、多级视图集成、模式集成、多粒度数据集成**。", "记作：**基、多视、模、多粒**。", """
### 概述

数据集成把来自多个数据源的数据合并为统一视图，使用户可以统一访问和理解。

### 核心内容

数据集成可以分为==基本数据集成==、==多级视图集成==、==模式集成==和==多粒度数据集成==四个层次。

### 易混点

基本数据集成常处理同一实体的识别与合并；模式集成常处理命名冲突和结构冲突。
"""),
    C("sec_12", "多级视图与模式集成", "comparison", 2, "57", ["系统集成", "数据集成", "视图集成"], "- 多级视图集成：本地视图 -> 中间视图 -> 集成视图。\n- 模式集成处理**命名冲突**和**结构冲突**。", "", """
### 概述

多级视图集成和模式集成都用于解决不同数据源之间的不一致。

### 核心内容

多级视图集成通常从本地视图映射到公共中间视图，再映射到集成视图。它涉及数据转换、语义冲突消除和数据导出。

模式集成主要解决==命名冲突==和==结构冲突==。

### 易混点

多级视图更强调视图映射链路；模式集成更强调数据结构和命名的一致化。
"""),
    C("sec_12", "异构数据集成方法", "formula", 2, "57", ["系统集成", "数据集成", "异构数据"], "- 异构数据集成方法：**过程式方法、声明式方法**。\n- 常见接口/格式：**ODBC、XML、JSON**。", "", """
### 概述

异构数据集成处理不同系统、不同数据模型或不同格式之间的数据互通。

### 核心内容

异构数据集成方法可归纳为==过程式方法==和==声明式方法==。常见相关技术和格式包括 ODBC、XML 和 JSON。

### 易混点

过程式方法强调如何执行集成过程；声明式方法强调描述想得到的结果或映射规则。
"""),
    C("sec_13", "代表性软件构件标准", "formula", 2, "58", ["系统集成", "软件集成", "构件标准"], "- 构件标准：**CORBA、COM、DCOM、COM+、.NET、J2EE**。", "", """
### 概述

软件集成常依赖构件标准和运行框架，解决跨语言、跨平台或分布式环境下的软件复用与互操作。

### 核心内容

代表性软件构件标准包括==CORBA==、==COM==、==DCOM==、==COM+==、==.NET==和==J2EE==。

### 易混点

DCOM 是 COM 的扩展，面向分布式环境；COM+ 是 COM 更高层次上的应用。
"""),
    C("sec_13", "构件标准易混点", "comparison", 3, "58", ["系统集成", "软件集成", "构件标准"], "- CORBA 强调平台互操作；COM 强调对象复用；DCOM 增加分布式特性；.NET 提供 CLR 和类库；J2EE 面向企业级分层应用。", "", """
### 概述

不同构件标准的关键词不同，选择题常通过关键词区分。

### 核心内容

| 标准 | 高频关键词 |
|:---|:---|
| ==CORBA== | OMG，跨平台互操作 |
| ==COM== | 跨语言、跨系统复用对象 |
| ==DCOM== | COM 扩展，位置透明、网络安全、跨平台调用 |
| ==COM+== | 基于 COM，提升到应用层，与操作系统结合更紧密 |
| ==.NET== | CLR、基础类库、数据库访问、网络开发 |
| ==J2EE== | 可伸缩、灵活、易维护的企业级体系结构 |

### 易混点

J2EE 常按客户端层、服务器端组件层、EJB 层和信息系统层理解。
"""),
    C("sec_14", "应用集成目标", "concept", 2, "58", ["系统集成", "应用集成", "EAI"], "- 应用集成位于系统集成栈上层，主要解决**应用互操作性**。\n- EAI：连接独立应用，实现协同工作。", "", """
### 概述

应用集成处在系统集成技术栈的上层，关注独立应用之间能否协同工作。

### 核心内容

从信息系统集成技术角度看，应用集成在集成栈的最上层，主要解决应用的==互操作性==问题。

应用集成或企业应用集成 EAI，是指将独立的软件应用连接起来，实现协同工作。

### 易混点

应用集成不是简单数据同步，它还包括跨应用功能服务的使用和协同。
"""),
    C("sec_14", "应用集成技术要求与组件", "formula", 2, "58,59", ["系统集成", "应用集成", "API"], "- 技术要求：**互操作性、可移植性、透明性**。\n- 协调组件：**API、事件驱动型操作、数据映射**。", "要求记：**互、移、透**。", """
### 概述

应用集成既要让应用彼此交换有意义的信息，也要降低分布式环境带来的复杂度。

### 核心内容

| 类别 | 枚举 |
|:---|:---|
| 技术要求 | ==应用间互操作性==、==分布式环境中应用的可移植性==、==系统中应用分布的透明性== |
| 协调组件 | ==API==、==事件驱动型操作==、==数据映射== |

### 易混点

透明性强调屏蔽系统分布带来的复杂性，使应用开发者不必关心系统是分布式还是集中式。
"""),
    C("sec_15", "信息安全三维空间", "formula", 2, "59", ["安全工程", "信息安全", "安全空间"], "- 三轴：X 轴**安全机制**，Y 轴**OSI 网络参考模型**，Z 轴**安全服务**。\n- 五大属性：**认证、权限、完整、加密、不可否认**。", "三轴记：**机制、模型、服务**。", """
### 概述

信息安全系统三维空间用安全机制、网络参考模型和安全服务三个维度描述安全体系。

### 核心内容

| 轴 | 含义 |
|:---|:---|
| ==X 轴== | 安全机制 |
| ==Y 轴== | OSI 网络参考模型 |
| ==Z 轴== | 安全服务 |

安全空间五大属性是==认证==、==权限==、==完整==、==加密==和==不可否认==。

### 易混点

三维空间的三个轴不是五大属性；五大属性是安全空间逐步扩展后形成的核心安全内涵。
"""),
    C("sec_15", "安全机制与安全技术", "formula", 2, "59,60", ["安全工程", "安全机制", "安全技术"], "- 安全机制包括：基础设施实体、平台、数据、通信、应用、运行、管理、授权和审计、安全防范体系。\n- 安全技术包括：加密、数字签名、访问控制、数据完整性、认证、数据挖掘。", "", """
### 概述

安全机制是安全服务背后的结构体系，安全技术是实现机制和服务的具体手段。

### 核心内容

安全机制包括基础设施实体安全、平台安全、数据安全、通信安全、应用安全、运行安全、管理安全、授权和审计安全、安全防范体系等。

安全技术主要涉及==加密==、==数字签名==、==访问控制==、==数据完整性==、==认证==和==数据挖掘==等。

### 易混点

安全机制偏体系结构；安全技术偏具体实现手段。
"""),
    C("sec_15", "六类安全服务", "formula", 2, "59,60", ["安全工程", "安全服务", "信息安全"], "- 安全服务：**对等实体认证、数据保密、数据完整性、数据源点认证、禁止否认、犯罪证据提供**。", "记作：**认、密、整、源、否、证**。", """
### 概述

安全服务描述系统向通信实体和用户提供哪些安全能力。

### 核心内容

安全服务包括==对等实体认证服务==、==数据保密服务==、==数据完整性服务==、==数据源点认证服务==、==禁止否认服务==和==犯罪证据提供服务==。

### 易混点

对等实体认证确认通信对方实体真实性；数据源点认证确认数据来自真正源点。
"""),
    C("sec_15", "信息安全系统建设内容", "formula", 2, "60", ["安全工程", "信息安全系统", "系统建设"], "- 建设内容：**硬件、软件、通信及网络、数据存储与灾备、系统、测试、密码、组织信息化工程**。", "", """
### 概述

信息安全系统建设不是单独安装安全产品，而是跨多个工程领域的系统性建设。

### 核心内容

信息安全系统建设主要包括==硬件工程==、==软件工程==、==通信及网络工程==、==数据存储与灾备工程==、==系统工程==、==测试工程==、==密码工程==和==组织信息化工程==。

### 易混点

安全策略需要由组织、客户和服务对象、集成商、安全产品开发者、密码研制单位、独立评估者等共同协调。
"""),
    C("sec_15", "ISSE-CMM 适用对象与过程", "formula", 3, "60,61", ["安全工程", "ISSE-CMM", "成熟度"], "- ISSE-CMM 是衡量信息安全系统工程实施能力的方法。\n- 适用：**工程组织、获取组织、评估组织**。\n- 过程：**工程过程、风险过程、保证过程**。", "", """
### 概述

ISSE-CMM 是信息安全系统工程能力成熟度模型，用面向工程过程的方法衡量安全系统工程实施能力。

### 核心内容

ISSE-CMM 主要适用于==工程组织==、==获取组织==和==评估组织==。

ISSE 将信息安全系统工程实施过程分解为==工程过程==、==风险过程==和==保证过程==。

### 易混点

ISSE-CMM 关注信息安全系统工程实施能力，不是普通软件过程能力模型 CSMM。
"""),
    C("sec_15", "ISSE-CMM 公共特性成熟度", "formula", 2, "61", ["安全工程", "ISSE-CMM", "成熟度"], "- 五级：**非正规实施、规划和跟踪、充分定义、量化控制、持续改进**。", "记作：**非、规、定、量、改**。", """
### 概述

ISSE-CMM 的公共特性成熟度等级描述组织安全工程过程从非正规到持续改进的演进。

### 核心内容

公共特性成熟度等级包括==Level 1 非正规实施级==、==Level 2 规划和跟踪级==、==Level 3 充分定义级==、==Level 4 量化控制级==、==Level 5 持续改进级==。

### 易混点

看到“量化控制”对应 Level 4；看到“持续改进”对应 Level 5。
"""),
    C("sec_15", "成熟度模型横向识别", "comparison", 3, "62-65", ["安全工程", "成熟度模型", "CMMI"], "- 第 5 章末尾成熟度总结用于横向识别模型，不建议逐字背长表。\n- 高频识别：**DCMM 管数据管理能力，CSMM 管软件过程能力，ISSE-CMM 管信息安全系统工程，CMMI 管能力成熟度集成**。", "", """
### 概述

本章末尾汇总了多个成熟度模型，学习重点是识别“哪个模型衡量什么”，而不是背诵长表原文。

### 核心内容

| 模型 | 识别关键词 |
|:---|:---|
| ==DCMM== | 数据管理能力成熟度 |
| ==CSMM== | 软件过程能力成熟度 |
| ==ISSE-CMM== | 信息安全系统工程能力成熟度 |
| ==OPM== | 组织项目管理成熟度 |
| ==CMMI== | 能力成熟度模型集成 |

### 易混点

题干出现“数据管理规范化”优先想 DCMM；出现“信息安全系统工程实施能力”优先想 ISSE-CMM。
"""),
]


def read_json(path: Path) -> dict:
    return json.loads(path.read_text(encoding="utf-8"))


def write_json(path: Path, data: dict) -> None:
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")


def sha256(path: Path) -> str:
    h = hashlib.sha256()
    with path.open("rb") as f:
        for chunk in iter(lambda: f.read(1024 * 1024), b""):
            h.update(chunk)
    return h.hexdigest()


def build_card_json(card: dict, sort_no: int, point_id: str) -> dict:
    stem = f"{CHAPTER_ID}_{card['section_id']}_{point_id}"
    return {
        "point_id": point_id,
        "subject_id": SUBJECT_ID,
        "chapter_id": CHAPTER_ID,
        "section_id": card["section_id"],
        "title": card["title"],
        "card_type": card["card_type"],
        "difficulty": card["difficulty"],
        "estimated_read_seconds": max(30, min(120, len(card["md"]) // 18)),
        "has_key_content": bool(card["key_points"] or "==" in card["md"]),
        "is_free": True,
        "sort_no": sort_no,
        "content_file": f"{stem}.md",
        "content_md_path": f"subjects/{SUBJECT_ID}/chapters/{CHAPTER_ID}/cards/{stem}.md",
        "tags": card["tags"],
        "key_points": card["key_points"],
        "mnemonics": card["mnemonics"],
        "warnings": "第五章已按 PDF 第48-65页与知识卡片规范重制；受 OCR 来源限制，仍建议上线前抽样核对原 PDF。",
        "prerequisite_point_ids": [],
        "related_point_ids": [],
        "related_question_ids": [],
        "source": "2026新版高项三色笔记（信息系统项目管理师）",
        "source_type": "exam_note",
        "source_refs": [
            {
                "source_id": "pdf_2026_tricolor_high",
                "page_label": card["page"],
            }
        ],
        "author": "Codex curated from color OCR",
        "reviewer": "待人工复核",
        "review_status": "ai_draft",
        "created_at": "2026-05-13T00:00:00+08:00",
        "updated_at": BUILD_TIME,
        "version": 3,
        "schema_version": 2,
    }


def rebuild_chapter_cards() -> Counter:
    CARDS_DIR.mkdir(parents=True, exist_ok=True)
    keep_files: set[str] = set()
    section_counts: Counter = Counter()

    for sort_no, card in enumerate(CARDS, start=1):
        point_id = f"{sort_no:03d}"
        stem = f"{CHAPTER_ID}_{card['section_id']}_{point_id}"
        json_path = CARDS_DIR / f"{stem}.json"
        md_path = CARDS_DIR / f"{stem}.md"
        md_path.write_text(card["md"], encoding="utf-8")
        write_json(json_path, build_card_json(card, sort_no, point_id))
        keep_files.update({json_path.name, md_path.name})
        section_counts[card["section_id"]] += 1

    for path in sorted(CARDS_DIR.glob("ch_05_*.*")):
        if path.name not in keep_files:
            path.unlink()

    return section_counts


def update_counts(section_counts: Counter) -> None:
    card_count = len(CARDS)

    chapter_meta_path = CHAPTER_ROOT / "chapter_meta.json"
    chapter_meta = read_json(chapter_meta_path)
    chapter_meta["description"] = "信息系统工程，由 PDF 第48-65页整理为移动端知识卡片。"
    chapter_meta["card_count"] = card_count
    for section in chapter_meta["sections"]:
        section["card_count"] = section_counts.get(section["section_id"], 0)
        section["description"] = SECTION_DESCRIPTIONS.get(section["section_id"], section.get("description", ""))
    write_json(chapter_meta_path, chapter_meta)

    subject_path = PUBLIC_ROOT / "subjects" / SUBJECT_ID / "subject_index.json"
    subject = read_json(subject_path)
    for chapter in subject["chapters"]:
        if chapter["chapter_id"] == CHAPTER_ID:
            chapter["card_count"] = card_count
            break
    subject["updated_at"] = BUILD_TIME
    write_json(subject_path, subject)

    manifest_path = PUBLIC_ROOT / "manifest.json"
    manifest = read_json(manifest_path)
    manifest["content_version"] = CONTENT_VERSION
    manifest["generated_at"] = BUILD_TIME
    manifest["updated_at"] = BUILD_TIME
    total = 0
    for chapter in manifest.get("chapters", []):
        if chapter["chapter_id"] == CHAPTER_ID:
            chapter["card_count"] = card_count
        total += chapter.get("card_count", 0)
    manifest["card_count"] = total
    write_json(manifest_path, manifest)


def rebuild_file_index() -> None:
    entries = []
    for path in sorted(PUBLIC_ROOT.rglob("*")):
        if path.is_file() and path.name != "file_index.json":
            entries.append(
                {
                    "path": path.relative_to(PUBLIC_ROOT).as_posix(),
                    "bytes": path.stat().st_size,
                    "sha256": sha256(path),
                }
            )
    write_json(
        PUBLIC_ROOT / "file_index.json",
        {
            "package_id": "atomq_public_content",
            "version": CONTENT_VERSION,
            "file_count": len(entries),
            "files": entries,
        },
    )


def main() -> None:
    expected = 72
    if len(CARDS) != expected:
        raise RuntimeError(f"expected {expected} cards, got {len(CARDS)}")
    section_counts = rebuild_chapter_cards()
    update_counts(section_counts)
    rebuild_file_index()
    print(f"rebuilt {CHAPTER_ID}: cards={len(CARDS)} section_counts={dict(section_counts)}")


if __name__ == "__main__":
    main()
