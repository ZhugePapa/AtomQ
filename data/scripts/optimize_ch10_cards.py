#!/usr/bin/env python3
"""Curate chapter 10 knowledge cards from the 2026 tricolor notes."""

from __future__ import annotations

import hashlib
import json
from collections import Counter
from datetime import datetime, timedelta, timezone
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]
PUBLIC_ROOT = ROOT / "content_package" / "public"
SUBJECT_ID = "high_itpmp"
CHAPTER_ID = "ch_10"
CHAPTER_ROOT = PUBLIC_ROOT / "subjects" / SUBJECT_ID / "chapters" / CHAPTER_ID
CARDS_DIR = CHAPTER_ROOT / "cards"
BUILD_TIME = datetime.now(timezone(timedelta(hours=8))).isoformat(timespec="seconds")
CONTENT_VERSION = "2026.05.full-color-ocr-draft.15.ch10-curated"


SECTION_TITLES = {
    "sec_01": "管理基础与过程框架",
    "sec_02": "定义活动与排列顺序",
    "sec_03": "估算活动持续时间",
    "sec_04": "制定计划与控制进度",
}


SECTION_DESCRIPTIONS = {
    "sec_01": "进度计划形成逻辑、新兴实践、六大过程、裁剪与进度管理计划。",
    "sec_02": "定义活动、分解、滚动式规划、活动清单、PDM、ADM、依赖关系和提前滞后量。",
    "sec_03": "活动持续时间估算、类比估算、参数估算、三点估算、自下而上估算和储备分析。",
    "sec_04": "关键路径法、浮动时间、资源优化、情景分析、进度压缩、PERT、发布规划和控制进度。",
}


def md(text: str) -> str:
    return text.strip() + "\n"


def C(
    section_id: str,
    title: str,
    card_type: str,
    difficulty: int,
    page: str,
    tags: list[str],
    key_points: str,
    mnemonics: str,
    body: str,
) -> dict:
    return {
        "section_id": section_id,
        "title": title,
        "card_type": card_type,
        "difficulty": difficulty,
        "page": page,
        "tags": tags,
        "key_points": key_points.strip(),
        "mnemonics": mnemonics.strip(),
        "md": md(body),
    }


CARDS = [
    C("sec_01", "进度管理的定位", "concept", 1, "104", ["进度管理", "进度计划", "进度模型"], "- 进度计划不是一次性文档，项目全过程都要根据**风险、知识、价值活动**持续维护。\n- 小项目中，定义活动、排序、估算持续时间、制定进度模型可由同一人短时间内完成。", "", """
### 概述

进度管理的目标，是让项目在可控的时间安排下交付成果。它不只是画一张甘特图，而是持续维护活动、逻辑关系、资源、持续时间和进度基准。

### 核心内容

对于小型项目，==定义活动==、==排列活动顺序==、==估算活动持续时间==和==制定进度模型==之间联系紧密，可能由一个人在较短时间内完成。

项目团队需要在整个项目期间维护进度计划的灵活性，并随着知识增加、风险理解加深、增值活动设计变化而调整进度细节。

### 易混点

进度计划不是“做完规划就不动”的静态成果。只要项目状态、风险或价值交付方式变化，进度模型就可能需要更新。
"""),
    C("sec_01", "进度计划形成步骤", "process", 1, "104", ["进度管理", "制定进度计划", "进度模型"], "- 进度计划形成顺序：**选方法 -> 输入项目数据 -> 生成进度模型 -> 形成进度计划**。", "", """
### 概述

制定进度计划前，需要先确定排程方法，再把活动、日期、资源和约束输入进度工具，形成可分析、可更新的进度模型。

### 核心内容

1. 选择进度计划方法，例如==关键路径法==。
2. 将项目特定数据输入进度计划工具，包括活动、计划日期、持续时间、资源、依赖关系和制约因素。
3. 创建项目进度模型。
4. 根据进度模型形成==项目进度计划==。

### 易混点

进度模型是用于计算和分析的模型；项目进度计划是面向执行、沟通和控制的计划成果。
"""),
    C("sec_01", "进度灵活性", "concept", 1, "104", ["进度管理", "灵活性", "风险"], "- 维护灵活性重点看三类变化：**知识增加、风险理解加深、增值活动变化**。", "", """
### 概述

进度计划要在可控和灵活之间取得平衡。项目越复杂，越不能把早期计划当作不可调整的承诺。

### 核心内容

项目团队在整个项目期间都要维护进度计划的灵活性。随着==知识增加==、对风险理解加深，以及增值活动设计变化，进度计划应允许合理调整。

### 易混点

灵活性不等于随意变更。影响进度基准的调整仍应按变更控制流程处理。
"""),
    C("sec_01", "迭代型进度计划", "concept", 2, "104", ["进度管理", "敏捷", "未完项"], "- 迭代型进度计划常与**未完项清单、滚动式规划、固定时间盒**一起出现。\n- 适合增量交付客户价值，或多个团队并行开发大型关联工作。", "", """
### 概述

迭代型进度计划常用于适应型生命周期。它不是一次性排完所有细节，而是在滚动式规划中逐步细化。

### 核心内容

迭代型进度计划将需求记录为==用户故事==，先构建未完项清单，再在施工或开发前对需求排序和优化。

团队在固定时间盒内开发产品功能，适合增量交付客户价值，或多个团队并行开发大量内部关联工作的场景。

### 易混点

迭代型计划不是没有计划，而是把详细计划放到近期迭代中，把远期工作保持在较粗粒度。
"""),
    C("sec_01", "按需进度计划", "concept", 2, "104", ["进度管理", "按需进度", "看板"], "- 按需进度计划：资源可用时，从**未完项清单/工作队列**中拉取任务。\n- 适合运营维护、持续增量交付、任务规模相近的工作。", "", """
### 概述

按需进度计划不预先制定完整详细进度，而是根据资源可用情况从工作队列中拉取任务。

### 核心内容

按需进度计划通常从==未完项清单==或==工作队列==中提取待完成任务，资源一旦可用就立即执行。

这种方法适合运营环境、持续增量开发、工作任务规模和范围相近、能够按大小或范围分组的场景。

### 易混点

按需进度计划不是项目进度管理的缺失，而是适用于持续流动型工作的排程方式。
"""),
    C("sec_01", "进度管理六大过程", "formula", 2, "104,105", ["进度管理", "管理过程", "过程组"], "- 六过程：**规划进度管理、定义活动、排列活动顺序、估算活动持续时间、制定进度计划、控制进度**。", "记作：**规、定、排、估、制、控**。", """
### 概述

进度管理通过六个过程，把范围成果转化为可执行、可监控、可控制的时间安排。

### 核心内容

| 过程组 | 进度管理过程 |
|:---|:---|
| 规划 | ==规划进度管理==、==定义活动==、==排列活动顺序==、==估算活动持续时间==、==制定进度计划== |
| 监控 | ==控制进度== |

### 易混点

控制进度属于监控过程组；前五个过程都属于规划过程组。
"""),
    C("sec_01", "六大过程目的与时机", "comparison", 3, "105", ["进度管理", "过程目的", "过程时机"], "- 定义活动、排序、估算、控制进度一般贯穿项目期间。\n- 制定进度计划通常在规划阶段集中开展，并在需要时滚动更新。", "", """
### 概述

考试常把过程名称、目的和开展时机混在一起问，需要按关键词识别。

### 核心内容

| 过程 | 目的抓手 |
|:---|:---|
| ==规划进度管理== | 为规划、编制、管理、执行和控制进度制定政策、程序和文档 |
| ==定义活动== | 识别并记录为完成可交付成果所需采取的具体行动 |
| ==排列活动顺序== | 识别并记录活动之间的关系，定义工作逻辑顺序 |
| ==估算活动持续时间== | 根据资源估算，估算完成单项活动所需工作时段数 |
| ==制定进度计划== | 分析活动顺序、持续时间、资源需求和制约因素，创建进度模型 |
| ==控制进度== | 监督项目状态，更新进度，管理进度基准变更 |

### 易混点

“维护进度基准”对应控制进度；“创建进度模型”对应制定进度计划。
"""),
    C("sec_01", "进度管理裁剪", "formula", 2, "105", ["进度管理", "裁剪", "资源可用性"], "- 裁剪考虑：**生命周期方法、资源可用性、项目维度、技术支持**。", "", """
### 概述

不同项目的排程粒度、工具、资源约束和生命周期不同，进度管理需要裁剪。

### 核心内容

进度管理裁剪可重点考虑==生命周期方法==、==资源可用性==、==项目维度==和==技术支持==。

### 易混点

资源是否共享、关键资源是否有限，会直接影响进度计划的详细程度和资源优化方式。
"""),
    C("sec_01", "适应型进度管理", "concept", 2, "105", ["进度管理", "适应型", "混合生命周期"], "- 大型组织可能同时存在预测型、适应型和混合型项目。\n- 组合/项目集层面要按团队规模、分布、合规、组织复杂性、技术复杂性等维度管理。", "", """
### 概述

在大型组织中，进度管理往往不是单一方法。项目组合和项目集可能同时包含预测型、适应型和混合型交付方式。

### 核心内容

敏捷或适应型环境下，可按==团队规模==、==地理分布==、==合规要求==、==组织复杂性==和==技术复杂性==等维度管理进度。

对组织级大型项目，完整交付生命周期中可以同时使用预测型、适应型或混合型方法。

### 易混点

敏捷并不排斥中长期进度管理；只是把详细承诺和滚动规划结合起来。
"""),
    C("sec_01", "规划进度管理 ITTO", "comparison", 2, "105", ["进度管理", "规划进度管理", "ITTO"], "- 输出只有一个核心成果：**进度管理计划**。\n- 工具：专家判断、数据分析、会议。", "", """
### 概述

规划进度管理决定后续进度如何规划、制定、执行和控制。

### 核心内容

| 类别 | 内容 |
|:---|:---|
| 输入 | 项目章程、项目管理计划、事业环境因素、组织过程资产 |
| 工具技术 | ==专家判断==、==数据分析==、==会议== |
| 输出 | ==进度管理计划== |

### 易混点

规划进度管理不直接输出进度基准；进度基准是制定进度计划的输出。
"""),
    C("sec_01", "进度管理计划", "concept", 2, "105,106", ["进度管理", "进度管理计划", "控制临界值"], "- 进度管理计划规定如何**制定、监督和控制进度**。\n- 常考内容：进度模型、发布/迭代长度、准确度、计量单位、WBS、维护规则、控制临界值、绩效测量规则、报告格式。", "", """
### 概述

进度管理计划是项目管理计划的组成部分，用来规定制定、监督和控制项目进度的准则和活动。

### 核心内容

进度管理计划可包括==项目进度模型==、发布和迭代长度、准确度、计量单位、WBS、进度模型维护规则、控制临界值、绩效测量规则和报告格式。

它可以是正式或非正式的，也可以非常详细或高度概括，取决于项目需要。

### 易混点

控制临界值用于判断偏差是否需要采取行动；绩效测量规则常与挣值管理结合。
"""),
    C("sec_02", "定义活动", "concept", 2, "106", ["进度管理", "定义活动", "活动清单"], "- 定义活动把工作包进一步细化为可执行活动，是估算、排序、执行、监督和控制的基础。", "", """
### 概述

定义活动用于识别并记录为完成可交付成果所需采取的具体行动。

### 核心内容

定义活动的作用是把工作包进一步细化为==进度活动==，为估算、排序、执行、监督和控制项目工作奠定基础。

输出包括活动清单、活动属性、里程碑清单、变更请求，以及项目管理计划更新。

### 易混点

WBS 的最低层是工作包；定义活动是在工作包基础上识别具体活动。
"""),
    C("sec_02", "分解", "concept", 2, "106", ["进度管理", "定义活动", "分解"], "- 每个 WBS 工作包都要分解成所需活动。\n- 让团队成员参与分解，有助于得到更准确的活动定义。", "", """
### 概述

分解是把项目范围和可交付成果逐步拆成更小、更便于管理的组成部分。

### 核心内容

在定义活动中，项目团队需要把每个 WBS 工作包分解成完成工作包所需的==活动==。

让团队成员参与分解，有助于得到更好、更准确的结果。

### 易混点

创建 WBS 时分解到工作包；定义活动时再把工作包分解为活动。
"""),
    C("sec_02", "滚动式规划", "concept", 2, "106", ["进度管理", "滚动式规划", "渐进明细"], "- 滚动式规划：近期工作详细规划，远期工作高层级规划。\n- 本质是**渐进明细**。", "", """
### 概述

滚动式规划是一种迭代式规划技术，适合项目早期信息不足、远期工作尚不清晰的场景。

### 核心内容

滚动式规划要求对近期要完成的工作进行==详细规划==，对远期工作只做==高层级规划==。

随着信息逐渐清晰，近期工作包会被分解为具体活动。

### 易混点

滚动式规划不是规划不完整，而是在信息逐步明确时进行渐进明细。
"""),
    C("sec_02", "活动清单", "concept", 1, "106", ["进度管理", "活动清单", "定义活动"], "- 活动清单要包含活动标识和工作范围说明，让团队成员知道需要完成什么工作。", "", """
### 概述

活动清单是定义活动过程的主要输出之一，用于列出项目所需的全部进度活动。

### 核心内容

活动清单应包括每项活动的==活动标识==和==工作范围说明==，使项目团队成员了解需要完成哪些工作。

### 易混点

活动清单列活动；WBS 列可交付成果和工作包。两者粒度和用途不同。
"""),
    C("sec_02", "活动属性", "concept", 2, "106", ["进度管理", "活动属性", "活动清单"], "- 活动属性会随项目推进逐步扩展，常包括前置/后续活动、逻辑关系、提前量/滞后量、资源需求、强制日期、制约因素、假设条件。", "", """
### 概述

活动属性用于扩展活动清单中的活动描述，支持排序、估算、资源配置和控制。

### 核心内容

活动属性可能包括活动描述、前置活动、后续活动、逻辑关系、==提前量和滞后量==、资源需求、强制日期、制约因素和假设条件。

活动属性会随项目推进而演进和更新。

### 易混点

活动清单回答“有哪些活动”；活动属性回答“这些活动有什么排程和管理特征”。
"""),
    C("sec_02", "里程碑清单", "concept", 1, "106", ["进度管理", "里程碑", "定义活动"], "- 里程碑持续时间为**零**。\n- 里程碑可分为强制性和选择性。", "", """
### 概述

里程碑表示项目中的重要时间点或事件，用于沟通、跟踪和控制。

### 核心内容

里程碑清单列出项目全部里程碑，并指出里程碑是==强制性==还是==选择性==。

里程碑的持续时间为==零==，例如合同要求的日期属于强制性里程碑。

### 易混点

里程碑不是活动，不消耗持续时间；它是重要节点。
"""),
    C("sec_02", "排列活动顺序", "concept", 2, "106,107", ["进度管理", "排列活动顺序", "逻辑关系"], "- 除首尾活动外，每项活动至少有一个前置活动和一个后续活动。\n- 目标是在约束条件下获得最高效率的逻辑顺序。", "", """
### 概述

排列活动顺序用于识别并记录项目活动之间的逻辑关系。

### 核心内容

除首尾活动外，每项活动通常都应至少有一个==前置活动==和一个==后续活动==，并形成适当的逻辑关系。

合理排序可以在项目约束条件下获得最高效率。

### 易混点

排序关注活动之间“先后关系”；估算活动持续时间关注每项活动需要多久。
"""),
    C("sec_02", "紧前关系绘图法 PDM", "concept", 2, "107", ["进度管理", "PDM", "单代号网络图"], "- PDM：节点表示活动，箭线表示逻辑关系，也称**单代号网络图/活动节点图**。", "", """
### 概述

紧前关系绘图法 PDM 是表示活动逻辑关系的常用方法。

### 核心内容

PDM 用==节点==表示活动，用==箭线==表示活动之间的逻辑关系，因此也称为单代号网络图或活动节点图。

它支持完成到开始、完成到完成、开始到开始、开始到完成四类逻辑关系。

### 易混点

PDM 是“节点表示活动”；ADM 是“箭线表示活动”。
"""),
    C("sec_02", "PDM 四种逻辑关系", "comparison", 3, "107", ["进度管理", "PDM", "逻辑关系"], "- 最常用：**FS 完成到开始**。\n- 最少使用：**SF 开始到完成**。", "四类记作：**完开、完完、开开、开完**。", """
### 概述

PDM 的四种关系是进度网络图选择题高频考点。

### 核心内容

| 关系 | 含义 | 考试抓手 |
|:---|:---|:---|
| ==FS 完成到开始== | 前置活动完成后，后续活动才能开始 | 最常用 |
| ==FF 完成到完成== | 前置活动完成后，后续活动才能完成 | 两者完成点相关 |
| ==SS 开始到开始== | 前置活动开始后，后续活动才能开始 | 两者开始点相关 |
| ==SF 开始到完成== | 前置活动开始后，后续活动才能完成 | 很少使用 |

### 易混点

“后续活动能否开始”看 FS 或 SS；“后续活动能否完成”看 FF 或 SF。
"""),
    C("sec_02", "箭线图法 ADM", "concept", 3, "107", ["进度管理", "ADM", "双代号网络图"], "- ADM：箭线表示活动，节点表示事件，也称**双代号网络图/活动箭线图**。", "", """
### 概述

箭线图法 ADM 是另一种网络图表示方法，常用于双代号网络图计算题。

### 核心内容

ADM 用==箭线==表示活动，用==节点==表示事件，因此也称为双代号网络图或活动箭线图。

基本原则包括：活动和事件应有唯一编码；任意两个活动的紧前事件和紧后事件编号不能完全相同；节点编号一般沿箭线方向递增。

### 易混点

ADM 中“活动在箭线上”；PDM 中“活动在节点上”。
"""),
    C("sec_02", "虚活动", "concept", 2, "107", ["进度管理", "虚活动", "ADM"], "- 虚活动用**虚箭线**表示，不消耗时间和资源，只用于表达依赖关系。", "", """
### 概述

虚活动是双代号网络图中的特殊活动，用于表达逻辑关系而非真实工作。

### 核心内容

虚活动通常用==虚箭线==表示，既不消耗时间，也不消耗资源，只用于表达活动之间的依赖关系。

### 易混点

虚活动不是“很短的活动”，而是没有实际工作量的逻辑占位。
"""),
    C("sec_02", "依赖关系四类", "comparison", 3, "107", ["进度管理", "依赖关系", "强制性依赖"], "- 强制性依赖 = 硬逻辑；选择性依赖 = 软逻辑。\n- 外部依赖不受项目团队控制；内部依赖受项目团队控制。", "", """
### 概述

依赖关系决定活动排序的约束来源，常与“谁能控制”一起考。

### 核心内容

| 类型 | 含义 |
|:---|:---|
| ==强制性依赖关系== | 法律、合同或工作本身性质决定，也称硬逻辑 |
| ==选择性依赖关系== | 基于最佳实践或偏好选择，也称软逻辑 |
| ==外部依赖关系== | 项目活动与非项目活动之间的依赖，通常不受项目团队控制 |
| ==内部依赖关系== | 项目活动之间的依赖，通常受项目团队控制 |

### 易混点

需要快速跟进时，优先审查选择性依赖关系，而不是强制性依赖关系。
"""),
    C("sec_02", "提前量与滞后量", "comparison", 2, "107", ["进度管理", "提前量", "滞后量"], "- 提前量通常表示为**负值**；滞后量通常表示为**正值**。", "", """
### 概述

提前量和滞后量用于细化活动之间的逻辑关系，让网络图更贴近真实工作。

### 核心内容

| 概念 | 含义 | 常见表示 |
|:---|:---|:---|
| ==提前量== | 相对于前置活动，后续活动可以提前开始的时间 | 负值 |
| ==滞后量== | 相对于前置活动，后续活动需要推迟的时间 | 正值 |

### 易混点

提前量是让后续活动更早发生；滞后量是人为或自然等待。
"""),
    C("sec_02", "项目进度网络图", "concept", 1, "107", ["进度管理", "网络图", "逻辑关系"], "- 项目进度网络图是活动之间**逻辑关系/依赖关系**的图形表示。", "", """
### 概述

项目进度网络图把活动之间的依赖关系可视化，是关键路径和浮动时间分析的基础。

### 核心内容

项目进度网络图是表示项目进度活动之间==逻辑关系==或==依赖关系==的图形。

它可以用 PDM 或 ADM 等方法绘制。

### 易混点

网络图主要表达逻辑关系，不一定按时间比例绘制；时标网络图才体现时间刻度。
"""),
    C("sec_03", "估算活动持续时间", "concept", 2, "108", ["进度管理", "持续时间估算", "资源日历"], "- 先估算工作量和资源数量，再结合项目日历、资源日历估算持续时间。\n- 持续时间估算不包括滞后量。", "", """
### 概述

估算活动持续时间用于估算完成单项活动所需的工作时段数。

### 核心内容

估算时通常先估算工作量和计划投入的资源数量，再结合==项目日历==和==资源日历==估算完成活动需要的工作时段。

估算活动持续时间应由最熟悉活动的个人或小组提供，并随着数据质量提高逐步细化。

### 易混点

持续时间估算是活动本身需要多久，不包括活动之间的滞后量。
"""),
    C("sec_03", "持续时间影响因素", "concept", 2, "108", ["进度管理", "持续时间", "资源"], "- 资源数量和技能会影响持续时间，但不是线性关系。\n- 部分工作受客观限制，不能靠加人无限压缩。", "", """
### 概述

活动持续时间受资源、技术、人员状态和工作性质影响。

### 核心内容

资源数量、资源技能水平、技术进步和人员动力都会影响持续时间，但资源增加与工期缩短并不总是线性关系。

部分工作受客观规律制约，不能仅靠增加资源压缩。

### 易混点

看到“加人一定缩短工期”的说法要警惕，项目中存在沟通成本、学习成本和边际收益递减。
"""),
    C("sec_03", "类比估算", "concept", 2, "108", ["进度管理", "类比估算", "历史数据"], "- 类比估算用类似活动或项目的**历史数据**估算当前活动。\n- 成本低、速度快，但准确性较低。", "", """
### 概述

类比估算适合项目早期或详细信息不足时快速估算。

### 核心内容

类比估算使用类似活动或项目的==历史数据==，来估算当前活动或项目的持续时间、成本等参数。

它通常成本较低、耗时较少，但准确性也较低。

### 易混点

类比估算依赖相似性和专家判断；不是用统计公式计算。
"""),
    C("sec_03", "参数估算", "concept", 2, "108", ["进度管理", "参数估算", "算法"], "- 参数估算用历史数据和项目参数建立**统计关系/算法**。\n- 准确性取决于模型成熟度和基础数据可靠性。", "", """
### 概述

参数估算通过项目参数和历史数据之间的关系来计算持续时间或成本。

### 核心内容

参数估算利用==历史数据==与==项目参数==之间的统计关系和算法，计算活动持续时间。例如用工作量乘以单位工时。

估算准确性取决于模型成熟度和数据可靠性。

### 易混点

类比估算是找相似项目作参考；参数估算是用参数关系进行计算。
"""),
    C("sec_03", "三点估算公式", "formula", 3, "108,111", ["进度管理", "三点估算", "PERT"], "- 三点估算三值：**乐观 To、最可能 Tm、悲观 Tp**。\n- 三角分布：**Te=(To+Tm+Tp)/3**。\n- Beta/PERT：**Te=(To+4Tm+Tp)/6**。", "记 PERT 权重：**1、4、1，中间最可能给四倍**。", """
### 概述

三点估算用乐观、最可能、悲观三个估计值处理不确定性。

### 核心内容

| 分布 | 公式 |
|:---|:---|
| ==三角分布== | Te = (To + Tm + Tp) / 3 |
| ==Beta/PERT 分布== | Te = (To + 4Tm + Tp) / 6 |

其中 To 为乐观时间，Tm 为最可能时间，Tp 为悲观时间。

### 易混点

PERT/Beta 公式给最可能时间 Tm 四倍权重，是计算题高频点。
"""),
    C("sec_03", "自下而上估算", "concept", 2, "108", ["进度管理", "自下而上估算", "WBS"], "- 自下而上估算从 WBS 较低层级开始估算，再逐层汇总。", "", """
### 概述

自下而上估算适合工作分解较充分、需要更细致估算的场景。

### 核心内容

自下而上估算先估算 WBS 较低层级组成部分的持续时间或成本，再逐层==汇总==到更高层级。

### 易混点

自下而上通常比类比估算更细致，但也更耗时。
"""),
    C("sec_03", "备选方案分析", "concept", 2, "108", ["进度管理", "备选方案分析", "数据分析"], "- 备选方案分析可比较资源能力、技能水平、进度压缩方法、工具，以及创建/租赁/购买等方案。", "", """
### 概述

估算活动持续时间时，可通过备选方案分析选择更合适的执行方式。

### 核心内容

备选方案分析可比较资源能力、技能水平、进度压缩方法、工具，以及==创建、租赁、购买==等方案对持续时间的影响。

### 易混点

备选方案分析不是只比较成本，也会比较时间、资源、风险和可行性。
"""),
    C("sec_03", "储备分析", "comparison", 3, "108,109", ["进度管理", "储备分析", "管理储备"], "- 应急储备：应对**已识别风险**，包含在进度基准内。\n- 管理储备：应对**未知-未知风险**，不包含在进度基准内，但属于总工期。", "", """
### 概述

储备分析用于处理进度不确定性，重点区分应急储备和管理储备。

### 核心内容

| 储备类型 | 应对对象 | 是否进基准 |
|:---|:---|:---|
| ==应急储备== | 已识别风险导致的已知-未知不确定性 | 包含在进度基准内 |
| ==管理储备== | 未计划、未识别的未知-未知工作 | 不包含在进度基准内 |

### 易混点

使用管理储备可能需要变更进度基准；应急储备本身已在基准中。
"""),
    C("sec_03", "持续时间估算", "concept", 1, "109", ["进度管理", "持续时间估算", "估算结果"], "- 持续时间估算是对完成活动所需工作时段数的**定量评估**，可用范围表示。", "", """
### 概述

持续时间估算是估算活动持续时间过程的核心输出。

### 核心内容

持续时间估算是对完成某项活动所需工作时段数的==定量评估==，可以给出单点值，也可以给出范围，例如“2 周 ± 2 天”。

### 易混点

持续时间估算本身不包括滞后量，滞后量属于活动关系调整。
"""),
    C("sec_03", "估算依据", "concept", 2, "109", ["进度管理", "估算依据", "持续时间估算"], "- 估算依据要清楚说明估算如何得出，包括假设、制约、范围、置信区间和风险。", "", """
### 概述

估算依据用于解释持续时间估算的来源、方法和可信度。

### 核心内容

估算依据应清晰、完整地说明估算是如何得出的，可包括估算方法、支持信息、假设条件、制约因素、估算区间、置信水平和相关风险。

### 易混点

只有估算数值而没有依据，不利于审查、沟通和后续控制。
"""),
    C("sec_03", "估算活动持续时间 ITTO", "comparison", 3, "108,109", ["进度管理", "估算活动持续时间", "ITTO"], "- 工具技术高频：**类比估算、参数估算、三点估算、自下而上估算、备选方案分析、储备分析**。", "", """
### 概述

估算活动持续时间的 ITTO 容易被选择题考工具和输出。

### 核心内容

| 类别 | 高频内容 |
|:---|:---|
| 输入 | 进度管理计划、范围基准、活动属性、活动清单、里程碑清单、资源需求、资源日历、风险登记册等 |
| 工具技术 | ==类比估算==、==参数估算==、==三点估算==、==自下而上估算==、数据分析、决策、会议 |
| 输出 | ==持续时间估算==、==估算依据==、项目文件更新 |

### 易混点

三点估算和 PERT 经常与制定进度计划一起出现，但也属于估算活动持续时间的核心技术。
"""),
    C("sec_04", "制定进度计划", "concept", 2, "109", ["进度管理", "制定进度计划", "进度基准"], "- 制定进度计划会反复修正持续时间估算、资源估算和进度储备。\n- 批准后的进度计划成为**进度基准**。", "", """
### 概述

制定进度计划通过分析活动顺序、持续时间、资源需求和制约因素，形成可执行的进度模型。

### 核心内容

制定进度计划是一个迭代过程，需要反复审查和修正持续时间估算、资源估算和进度储备。

项目进度计划获得批准后，就成为用于跟踪绩效的==进度基准==。

### 易混点

进度计划和进度基准不是同义词；批准后的进度计划才成为基准。
"""),
    C("sec_04", "制定进度计划 ITTO", "comparison", 3, "109,110", ["进度管理", "制定进度计划", "ITTO"], "- 输出：**进度基准、项目进度计划、进度数据、项目日历**。\n- 工具重点：关键路径法、资源优化、假设情景分析、模拟、进度压缩、PERT。", "", """
### 概述

制定进度计划的工具和输出很多，是第十章选择题和计算题的核心区域。

### 核心内容

| 类别 | 高频内容 |
|:---|:---|
| 工具技术 | 进度网络分析、==关键路径法==、资源优化、数据分析、提前量和滞后量、进度压缩、PERT、项目管理信息系统、敏捷发布规划 |
| 输出 | ==进度基准==、==项目进度计划==、==进度数据==、==项目日历==、变更请求、计划和文件更新 |

### 易混点

进度基准、项目进度计划、进度数据和项目日历都是制定进度计划的重要输出。
"""),
    C("sec_04", "关键路径法", "concept", 3, "110", ["进度管理", "关键路径法", "关键路径"], "- 关键路径是从开始到完成的**最长路径**，决定项目最短工期。\n- 一个项目可以有多条关键路径。", "", """
### 概述

关键路径法用于估算项目最短工期，并识别活动的进度灵活性。

### 核心内容

关键路径是项目中从开始到完成的==最长路径==，决定了项目可能完成的最短时间。

关键路径上的活动通常没有总浮动时间；项目也可能存在多条关键路径。

### 易混点

关键路径是“最长路径”，但它决定“最短项目工期”。这两个词很容易被题目反向干扰。
"""),
    C("sec_04", "总浮动与自由浮动", "formula", 3, "110", ["进度管理", "浮动时间", "网络图计算"], "- 总浮动时间：**LS-ES 或 LF-EF**。\n- 自由浮动时间：不延误任何后续活动最早开始时间的可延迟量。", "", """
### 概述

浮动时间用于判断活动可以延迟多久而不影响项目或后续活动。

### 核心内容

| 类型 | 含义 | 常用公式 |
|:---|:---|:---|
| ==总浮动时间== | 活动可延迟但不延误项目完成日期或违反制约因素的时间 | LS - ES 或 LF - EF |
| ==自由浮动时间== | 活动可延迟但不延误任何后续活动最早开始日期的时间 | 后续活动最小 ES - 本活动 EF |

### 易混点

总浮动看项目完成或约束；自由浮动看后续活动最早开始。
"""),
    C("sec_04", "资源平衡与资源平滑", "comparison", 3, "110", ["进度管理", "资源优化", "资源平衡"], "- 资源平衡可能改变关键路径和项目完成日期。\n- 资源平滑不改变关键路径，通常不延误完成日期。", "", """
### 概述

资源优化用于在资源限制下调整进度计划，重点区分资源平衡和资源平滑。

### 核心内容

| 技术 | 含义 | 影响 |
|:---|:---|:---|
| ==资源平衡== | 根据资源制约调整开始和完成日期 | 可能改变关键路径，可能延误完成日期 |
| ==资源平滑== | 在不超过资源限制的前提下调整活动 | 不改变关键路径，活动只在浮动时间内延迟 |

### 易混点

资源平衡更“强”，可能动关键路径；资源平滑更“温和”，通常不动关键路径。
"""),
    C("sec_04", "假设情景分析与模拟", "comparison", 2, "110,111", ["进度管理", "数据分析", "蒙特卡洛"], "- 假设情景分析问“如果发生某情况会怎样”。\n- 模拟常见方法是**蒙特卡洛分析**。", "", """
### 概述

数据分析用于评估不确定性对进度目标的影响。

### 核心内容

| 技术 | 用途 |
|:---|:---|
| ==假设情景分析== | 评估某种情景发生时对项目目标的影响，可用于评估进度可行性和准备应对计划 |
| ==模拟== | 建模单个项目风险和其他不确定性，计算可能的进度结果 |

### 易混点

蒙特卡洛分析是模拟技术中的常见方法，不是简单类比。
"""),
    C("sec_04", "进度压缩", "concept", 2, "111", ["进度管理", "进度压缩", "关键路径"], "- 进度压缩是在不缩减项目范围的前提下缩短工期。\n- 常用方法：**赶工、快速跟进**。", "", """
### 概述

进度压缩用于在不减少范围的情况下缩短或加快项目持续时间。

### 核心内容

进度压缩的常用技术包括==赶工==和==快速跟进==。

它通常针对关键路径活动，因为非关键路径活动压缩后不一定能缩短项目总工期。

### 易混点

减少范围不是进度压缩；进度压缩强调范围不变。
"""),
    C("sec_04", "赶工与快速跟进", "comparison", 3, "111", ["进度管理", "赶工", "快速跟进"], "- 赶工：加资源、加成本，压缩可被资源缩短的关键路径活动。\n- 快速跟进：并行原本顺序执行的活动，增加返工和风险。", "", """
### 概述

赶工和快速跟进都能压缩进度，但代价不同。

### 核心内容

| 技术 | 做法 | 主要代价 |
|:---|:---|:---|
| ==赶工== | 增加资源、加班、支付加急费用等 | 增加成本和风险 |
| ==快速跟进== | 将原本顺序执行的活动或阶段并行开展 | 增加返工、协调和质量风险 |

### 易混点

赶工偏“花钱加资源”；快速跟进偏“改逻辑并行做”。
"""),
    C("sec_04", "PERT 估算", "formula", 3, "111", ["进度管理", "PERT", "三点估算"], "- PERT 使用三点估算，常用公式：**Te=(To+4Tm+Tp)/6**。", "记住：PERT 给**最可能时间 Tm 四倍权重**。", """
### 概述

PERT 是基于三点估算的进度分析技术，常用于考虑活动持续时间不确定性。

### 核心内容

PERT 使用乐观时间 To、最可能时间 Tm 和悲观时间 Tp，常用期望持续时间公式为：

==Te = (To + 4Tm + Tp) / 6==

### 易混点

如果题目明确说三角分布，用平均值公式；如果说 PERT 或 Beta 分布，用 1:4:1 公式。
"""),
    C("sec_04", "敏捷发布规划", "concept", 2, "111", ["进度管理", "敏捷发布规划", "迭代"], "- 敏捷发布规划通常规划 3-6 个月的发布节奏，确定发布包含的迭代或冲刺数量。", "", """
### 概述

敏捷发布规划基于产品路线图和产品愿景，对发布节奏和每次发布的价值交付进行高层级安排。

### 核心内容

敏捷发布规划通常确定==3-6 个月==内的发布计划，以及每次发布包含的迭代或冲刺数量。

它帮助产品负责人和团队根据目标、依赖关系和障碍决定发布内容与发布时间。

### 易混点

敏捷发布规划不是每日计划，而是高层级发布节奏安排。
"""),
    C("sec_04", "项目进度计划表现形式", "comparison", 2, "112", ["进度管理", "项目进度计划", "甘特图"], "- 甘特图最直观常用；里程碑图只显示关键节点；网络图强调逻辑关系。", "", """
### 概述

项目进度计划是进度模型的输出，展示计划日期、持续时间、里程碑和所需资源。

### 核心内容

| 表现形式 | 特点 |
|:---|:---|
| ==横道图/甘特图== | 活动在纵轴，日期在横轴，易读、常用 |
| ==里程碑图== | 只显示主要可交付成果和关键外部接口的开始或完成日期 |
| ==项目进度网络图== | 展示活动逻辑关系，可不按时间比例，也可做成时标网络图 |

### 易混点

甘特图便于沟通进度；网络图便于分析依赖、关键路径和浮动时间。
"""),
    C("sec_04", "项目日历", "concept", 1, "112", ["进度管理", "项目日历", "工作日"], "- 项目日历规定可开展进度活动的**工作日和班次**，区分可用和不可用时间。", "", """
### 概述

项目日历用于说明项目活动何时可以开展。

### 核心内容

项目日历规定可以开展进度活动的==工作日==和==班次==，用于区分可用时间和不可用时间。

### 易混点

项目日历关注项目整体可工作时间；资源日历关注具体资源何时可用。
"""),
    C("sec_04", "控制进度", "concept", 2, "112,113", ["进度管理", "控制进度", "进度基准"], "- 控制进度属于整体变更控制的一部分，重点是状态、影响因素、储备、变更和实际变更管理。", "", """
### 概述

控制进度用于监督项目状态，更新项目进度，并管理进度基准变更。

### 核心内容

控制进度重点关注：确定当前进度状态；影响造成进度变更的因素；重新考虑必要的进度储备；判断项目进度是否已经发生变更；在变更实际发生时进行管理。

控制进度是==实施整体变更控制==的一部分。

### 易混点

控制进度不是单纯记录进度，而是管理进度偏差和基准变更。
"""),
    C("sec_04", "控制进度数据分析", "comparison", 3, "113", ["进度管理", "控制进度", "数据分析"], "- EVA 用 SV/SPI 衡量偏离基准程度。\n- 迭代燃尽图跟踪剩余工作并预测偏差。\n- 趋势分析看绩效随时间变化。", "", """
### 概述

控制进度常用数据分析技术判断项目是否偏离计划，以及是否需要纠偏。

### 核心内容

| 技术 | 判断关键词 |
|:---|:---|
| ==挣值分析== | 使用 SV、SPI 等指标评估相对进度基准的偏差 |
| ==迭代燃尽图== | 跟踪迭代未完项中剩余工作，比较理想燃尽线和实际剩余工作 |
| ==绩效审查== | 比较实际开始/完成日期、实际持续时间、剩余持续时间和浮动时间 |
| ==趋势分析== | 检查绩效随时间变化，判断是改善还是恶化 |
| ==偏差分析== | 分析目标日期与实际日期、实际持续时间与计划持续时间的差异 |
| ==假设情景分析== | 根据不同情景预测对进度目标的影响 |

### 易混点

SV/SPI 属于挣值分析；燃尽图常用于迭代型或敏捷场景。
"""),
    C("sec_04", "缩短工期方法", "formula", 2, "114", ["进度管理", "缩短工期", "进度优化"], "- 缩短工期常见方法：**赶工、快速跟进、使用高质量资源、减小范围或降低要求、改进方法技术、加强质量管理减少返工**。", "", """
### 概述

缩短工期需要区分“压缩进度”和“改变范围/质量要求”的不同手段。

### 核心内容

常见缩短工期方法包括==赶工==、==快速跟进==、使用高素质或经验更丰富的资源、减小活动范围或降低活动要求、改进方法或技术、加强质量管理以减少返工。

### 易混点

如果题目明确要求“不缩减范围”，优先考虑赶工或快速跟进；如果允许改变范围，才考虑减小范围或降低要求。
"""),
]


def read_json(path: Path) -> dict:
    return json.loads(path.read_text(encoding="utf-8"))


def write_json(path: Path, data: dict) -> None:
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")


def sha256(path: Path) -> str:
    h = hashlib.sha256()
    with path.open("rb") as f:
        for chunk in iter(lambda: f.read(1024 * 1024), b""):
            h.update(chunk)
    return h.hexdigest()


def build_card_json(card: dict, sort_no: int, point_id: str) -> dict:
    stem = f"{CHAPTER_ID}_{card['section_id']}_{point_id}"
    return {
        "point_id": point_id,
        "subject_id": SUBJECT_ID,
        "chapter_id": CHAPTER_ID,
        "section_id": card["section_id"],
        "title": card["title"],
        "card_type": card["card_type"],
        "difficulty": card["difficulty"],
        "estimated_read_seconds": max(30, min(120, len(card["md"]) // 18)),
        "has_key_content": "==" in card["md"],
        "is_free": True,
        "sort_no": sort_no,
        "content_file": f"{stem}.md",
        "content_md_path": f"subjects/{SUBJECT_ID}/chapters/{CHAPTER_ID}/cards/{stem}.md",
        "tags": card["tags"],
        "key_points": card["key_points"],
        "mnemonics": card["mnemonics"],
        "warnings": "第十章已按 PDF 页码 104-114 与知识卡片规范重制；三点估算公式按项目管理通用口径校正。",
        "prerequisite_point_ids": [],
        "related_point_ids": [],
        "related_question_ids": [],
        "source": "2026新版高项三色笔记（信息系统项目管理师）",
        "source_type": "exam_note",
        "source_refs": [
            {
                "source_id": "pdf_2026_tricolor_high",
                "page_label": card["page"],
            }
        ],
        "author": "Codex curated from PDF notes",
        "reviewer": "待人工复核",
        "review_status": "ai_draft",
        "created_at": "2026-05-13T00:00:00+08:00",
        "updated_at": BUILD_TIME,
        "version": 3,
        "schema_version": 2,
    }


def rebuild_chapter_cards() -> Counter:
    CARDS_DIR.mkdir(parents=True, exist_ok=True)
    keep_files: set[str] = set()
    section_counts: Counter = Counter()

    for sort_no, card in enumerate(CARDS, start=1):
        point_id = f"{sort_no:03d}"
        stem = f"{CHAPTER_ID}_{card['section_id']}_{point_id}"
        json_path = CARDS_DIR / f"{stem}.json"
        md_path = CARDS_DIR / f"{stem}.md"
        md_path.write_text(card["md"], encoding="utf-8")
        write_json(json_path, build_card_json(card, sort_no, point_id))
        keep_files.update({json_path.name, md_path.name})
        section_counts[card["section_id"]] += 1

    for path in sorted(CARDS_DIR.glob("ch_10_*.*")):
        if path.name not in keep_files:
            path.unlink()

    return section_counts


def update_counts(section_counts: Counter) -> None:
    card_count = len(CARDS)

    chapter_meta_path = CHAPTER_ROOT / "chapter_meta.json"
    chapter_meta = read_json(chapter_meta_path)
    chapter_meta["description"] = "进度管理，由 PDF 第104-114页整理为移动端知识卡片。"
    chapter_meta["card_count"] = card_count
    for section in chapter_meta["sections"]:
        section_id = section["section_id"]
        section["title"] = SECTION_TITLES.get(section_id, section.get("title", ""))
        section["card_count"] = section_counts.get(section_id, 0)
        section["description"] = SECTION_DESCRIPTIONS.get(section_id, section.get("description", ""))
    write_json(chapter_meta_path, chapter_meta)

    subject_path = PUBLIC_ROOT / "subjects" / SUBJECT_ID / "subject_index.json"
    subject = read_json(subject_path)
    for chapter in subject["chapters"]:
        if chapter["chapter_id"] == CHAPTER_ID:
            chapter["card_count"] = card_count
            break
    subject["updated_at"] = BUILD_TIME
    write_json(subject_path, subject)

    manifest_path = PUBLIC_ROOT / "manifest.json"
    manifest = read_json(manifest_path)
    manifest["content_version"] = CONTENT_VERSION
    manifest["generated_at"] = BUILD_TIME
    manifest["updated_at"] = BUILD_TIME
    total = 0
    for chapter in manifest.get("chapters", []):
        if chapter["chapter_id"] == CHAPTER_ID:
            chapter["card_count"] = card_count
        total += chapter.get("card_count", 0)
    manifest["card_count"] = total
    write_json(manifest_path, manifest)


def rebuild_file_index() -> None:
    entries = []
    for path in sorted(PUBLIC_ROOT.rglob("*")):
        if path.is_file() and path.name != "file_index.json":
            entries.append(
                {
                    "path": path.relative_to(PUBLIC_ROOT).as_posix(),
                    "bytes": path.stat().st_size,
                    "sha256": sha256(path),
                }
            )
    write_json(
        PUBLIC_ROOT / "file_index.json",
        {
            "package_id": "atomq_public_content",
            "version": CONTENT_VERSION,
            "file_count": len(entries),
            "files": entries,
        },
    )


def main() -> None:
    expected = 51
    if len(CARDS) != expected:
        raise RuntimeError(f"expected {expected} cards, got {len(CARDS)}")
    section_counts = rebuild_chapter_cards()
    update_counts(section_counts)
    rebuild_file_index()
    print(f"rebuilt {CHAPTER_ID}: cards={len(CARDS)} section_counts={dict(section_counts)}")


if __name__ == "__main__":
    main()
